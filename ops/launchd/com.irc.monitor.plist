<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.irc.monitor</string>
  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>__REPO_ROOT__/ops/launchd/run-monitor.sh</string>
  </array>
  <key>WorkingDirectory</key>
  <string>__REPO_ROOT__</string>
  <!-- Daily 12:15 (Asia/Shanghai; machine is UTC+8). Fires every calendar day; the
       wrapper's trading-day gate still skips weekends + CN holidays, and a
       once-per-day idempotency guard skips if today's report.html already exists. -->
  <key>StartCalendarInterval</key>
  <dict>
    <key>Hour</key><integer>12</integer>
    <key>Minute</key><integer>15</integer>
  </dict>
  <key>RunAtLoad</key>
  <false/>
  <!-- launchd must NOT own a persistent log file: once `irc` writes to it the file
       gets the macOS com.apple.provenance xattr and launchd is denied reopening
       it on the next spawn (→ EX_CONFIG / 78, no run). The wrapper writes its own
       fresh per-run log under outputs/_logs/run-monitor.<ts>.log instead. -->
  <key>StandardOutPath</key>
  <string>/dev/null</string>
  <key>StandardErrorPath</key>
  <string>/dev/null</string>
</dict>
</plist>
