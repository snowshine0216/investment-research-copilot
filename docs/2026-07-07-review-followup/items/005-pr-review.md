Verdict: PASS-WITH-NITS

Source: /code-review on PR #209
PR comment URL: none (inline findings)
Findings: 3

- src/irc/rotation/seed.py:104-105 (`done` accumulation / `unresolved` detection) vs src/irc/monitor/industry_map_store.py:69-73 (`merge_seen`) — nit — `seed_stock_board_map`'s "is this symbol resolved" predicates are truthy-based (`if v` for `done`; `not industry_by_symbol.get(sym)` for `unresolved`), but the persistence gate in `merge_seen` is stripped-truthy (`isinstance(ind, str) and ind.strip()`). A whitespace-only industry string (e.g. `"  "`) would be truthy in seed.py — counted into `done` and *not* flagged `unresolved` — yet `merge_seen` would silently skip persisting it (fails its `.strip()` check), reproducing for that narrow input the exact "stale row survives unhealed with zero log" gap this PR's second fix (commit `77426054`) was written to close. Not currently reachable in production: the only real `batch_fetch` implementation, `fetch_flow_today_batch` (`src/irc/monitor/flow_batch_fetch.py:45-53`, `_coerce_industry`), already normalizes missing/blank/whitespace-only `f100` values to `None` before this function ever sees them. Worth aligning the two predicates (or importing a shared "resolved" check) so a future `batch_fetch` implementation can't silently reopen this class of bug; no test currently exercises a whitespace-only value.
- CHANGELOG.md (`### Fixed` block added by this PR, ~line 9-20) — nit — documents only the skip-set freshness fix (commit `a7bfeeed`, R-4). The ship-review hardening in the second commit (`77426054`: run-level unresolved-symbols warning + `chunk_size=0` → 1-symbol-chunk crash guard) is described in the PR body but has no CHANGELOG entry, so a reader of `[Unreleased]` alone would not learn that `seed_stock_board_map` can now log an "unresolved" warning or that a previously-reachable `ValueError` (via `IRC_ROTATION_TOPUP_BUDGET=0` → `range(step=0)`) was fixed.
- src/irc/rotation/seed.py:90 (`effective_chunk_size = max(1, chunk_size)`) — nit — a misconfigured `IRC_ROTATION_TOPUP_BUDGET=0` silently clamps to the worst-case call pattern (1-symbol chunks = maximum network-call count for the whole pending set) with no log line marking that the clamp fired. Consistent with this system's already-known, already-triaged naming quirk (`docs/2026-07-07-workflow-review.md` R-11, P3: "`IRC_ROTATION_TOPUP_BUDGET` is actually the chunk size — lowering the budget increases calls"), so this isn't a new deviation, but an operator who sets `0` expecting to reduce/pause load gets the opposite with zero observability. A one-line warning when the clamp fires (mirroring this PR's own "don't fail silently" theme) would close the gap cheaply.

## Verification performed independently

- Read full diff (`gh pr diff 209`, 7 files: CHANGELOG.md, 4 `docs/2026-07-07-review-followup/items/*.md`, `src/irc/rotation/seed.py`, `tests/rotation/test_seed.py`).
- Confirmed the core fix: `fresh = set(fresh_slice(existing, today))` replaces `set(existing.keys())`; `fresh_slice` (`src/irc/monitor/industry_map_store.py:89-93`) filters by `_within(seen_at, today, 30)` — stale entries (>30 calendar days) fall out of the skip-set and re-enter `pending`, so `record(...)`'s refresh-on-seen (`merge_seen`) bumps `seen_at` back to `today` on re-fetch. Traced end-to-end against `industry_map_store.py`; matches the stated coverage-self-heals behavior.
- Confirmed the two ship-review hardenings in commit `77426054`: (1) `unresolved` is accumulated per-chunk from symbols whose `industry_by_symbol` value is falsy, and a single run-level `_log.warning` fires once (not per-chunk) with a count + sorted sample of ≤5 symbols; (2) `effective_chunk_size = max(1, chunk_size)` prevents `range(0, n, 0)` `ValueError` for `chunk_size=0`. Confirmed `chunk_size=0` is genuinely reachable in production: `_topup_budget()` (`src/irc/commands/rotation_cmd.py:204-211`) does `int(os.environ.get("IRC_ROTATION_TOPUP_BUDGET", "50"))`, which happily returns `0` for an explicit `"0"` (only non-numeric strings fall back to the default via the `ValueError` catch).
- Independently reproduced RED→GREEN is plausible: checked current `seed.py` against all 4 new/changed tests in `tests/rotation/test_seed.py` (`test_seed_stock_board_map_refetches_stale_skips_fresh`, `test_seed_stock_board_map_roundtrip_refreshes_stale_seen_at`, `test_seed_stock_board_map_warns_once_on_unresolved_symbols`, `test_seed_stock_board_map_chunk_size_zero_does_not_crash`) — hand-traced each against the implementation; all assertions hold.
- Ran the affected test surface: `uv run pytest tests/rotation/test_seed.py -q` → 9 passed; `uv run pytest tests/monitor/test_industry_map_store.py tests/commands/test_rotation_cmd.py -q` → 23 passed (per-file, never the whole `tests/commands/` dir, per FACTS.md/CLAUDE.md convention).
- `uv run ruff check src/irc/rotation/seed.py tests/rotation/test_seed.py` → All checks passed.
- Confirmed no VERSION/`radar_version`/`schema_version` bump: `VERSION` = `0.9.3` unchanged; `src/irc/rotation/types.py:58-59` (`schema_version`/`radar_version`) untouched; diff name-only list contains no `types.py`/`VERSION` entry.
- Confirmed functional/immutability conventions: `fresh_slice`/`merge_seen` are pure (no mutation of `existing`/`store`); `effective_chunk_size` is a pure local computation; the only effects (`_log.warning`, `batch_fetch`, `record`) are confined to this edge-orchestration module, consistent with its own module docstring and the CLAUDE.md effects-at-edges convention. Local `from irc.monitor.industry_map_store import fresh_slice` import mirrors the existing lazy-import pattern already used for the same module in `rotation_cmd.py:219` — not a new/inconsistent pattern, and no import cycle exists (`irc.monitor` does not import from `irc.rotation`).
- Checked PR comments/reviews via `gh api repos/snowshine0216/investment-research-copilot/issues/209/comments` → `[]` (0 comments); `gh pr view 209 --json reviews,statusCheckRollup` → both empty. No CI configured/run against this PR; no prior GitHub-side review to cross-check against.

No blockers, no bugs found. All 3 findings above are nits: one is a latent (currently unreachable) predicate-consistency gap, one is a documentation-completeness gap (CHANGELOG missing the second commit), one is a pre-existing, already-triaged (R-11, P3) design quirk this PR's crash-guard inherits without adding operator-facing logging.
