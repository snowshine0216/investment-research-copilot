# SKIPPED — Monitor Eval M0 + M1

No items skipped.

Both milestones in the source spec (M0 eval spine, M1 LLM suites) are IN-scope. The source
spec's §9 "Out of scope" list (retro backtest, ledger scorer, ablation, the ADR, property-based
deterministic suites, `irc eval --live` aggregate, weight/band changes) are **M2–M4 milestones**
tracked in the parent roadmap — they are not deferred sub-tasks of this run, so they do not
appear here.
