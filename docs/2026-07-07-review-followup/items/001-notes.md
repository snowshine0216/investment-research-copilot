# Item 001 — implementation notes (deviations + triage)

- T1: the plan's verbatim `_flow_items` code contradicted its own test AND locked G-Q5→B (uniform staleness must render run-level lag) — implementer added the `at_newest < total` guard; reviewer independently confirmed spec-faithful vs the spec's minority-outlier rationale. conservative → triage: accepted.
- T3: `health` import placed in the top import group instead of the brief's mid-file placement (ruff E402 convention). conservative → triage: accepted. (`RunKind` gained "flow-capture" one task ahead of the cli Choice — plan-internal sequencing, typed-only.)
- T4: the brief's unconditional health attachment broke 3 pre-existing tests that staged monitor.json with no health artifacts — fixtures ENRICHED to production shape (co-present eval_trace/gold_regime) instead of weakening the wiring; reviewer verified against monitor_cmd's real write ordering that missing-eval_trace is a reachable state and health_unknown is the intended rendering. conservative → triage: accepted. Review fix round: weekly cold-machine path now attaches health_unknown (`4c8b739c`).
- T6: one factual correction extended beyond the brief's literal edit list (ops/launchd/README rotation timeout rows falsely said "never pages") — needed for same-table self-consistency; reviewer confirmed the paging trace TRUE in code. accepted. Review fix round: docs/monitor/README.md:44 no-page contradiction (`8976b484`).
- T7: AC1's illustrative "clean" superseded by locked G-Q5→B on the real flow store (688072 lag → degraded is correct) — resolved at plan time, evidence records both.
- Process: T3 implementer was cut off by a transient API server error mid-task (nothing persisted); resumed via SendMessage with context intact, completed normally. T5 wrapper work validated with bash -n + the ops test suite per-file.
