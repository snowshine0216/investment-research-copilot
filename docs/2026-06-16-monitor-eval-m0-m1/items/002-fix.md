# 002 fix log

Two fix rounds; all three post-ship verdicts PASS / PASS-WITH-NITS with no blockers or latent bugs.

## Round 1 — pre-push (during /ship steps 8+9; see items/002-ship-blocked.md)
6 real P0/P1 bugs in new code fixed BEFORE the PR opened (commits ded3a44, 8726780, 5eb6284);
1 adversarial P0 (worst_status/SKIPPED) investigated and REJECTED as a false positive.
- staleness resolve_health(None) wrong stage → missing report now fails open (caveated) ·
  fromisoformat crash on malformed ran_at guarded · scorers no longer vacuously pass degraded
  outputs (injection_resistance, hallucination_rate, magnitude_band_pass) · runners' unguarded
  record_command_run wrapped · narrative injection scorer added · drive_case logging.
Re-verified: 165 passed, ruff clean.

## Round 2 — post-ship nits (from /code-review, items/002-pr-review.md)
The 3 PR-review nits were the SAME vacuous-pass class in 3 more aggregating scorers — non-blocking
(suite still FAILs overall in all-degraded runs) but inconsistent with spec §5, so fixed for
robustness (commit 8fa208c):
- citation_resolution + citation_validity: no claim/impact rows across all outputs → 0.0 (FAIL),
  not vacuous 1.0.
- attribution_honesty: degraded (empty-claims) case counts as a miss, not a vacuous hit.
- Preserved: genuinely-empty-case-set path (no cases of the category → 1.0) and valid-output-with-
  zero-citation_ids path — only the "cases exist but all degraded" path changed.
Re-verified: 133 passed, ruff clean.

## Exit state
- /verify: PASS (21/21 acceptance criteria)
- review (/ship 8+9 inline): PASS
- pr-review (/code-review): PASS-WITH-NITS → all 3 nits now fixed
No blockers, no latent bugs. Ready for merge.
