# SKIPPED — Pickability Follow-ups

_None at run start._

All three deferred items from the prior `instrument-pickability` run (F4, F5, F6) are IN-scope this run per the user's explicit decision at intake. If a per-item grill phase discovers a hard pre-requisite that cannot be resolved inside this run (e.g., F5's eval bench requires an LLM corpus that doesn't exist and cannot be built today), the item is moved here with a written reason and a recommended unblock path — never silently abandoned.
