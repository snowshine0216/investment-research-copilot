Verdict: PASS

Subagent: grill-with-docs (autonomous — no user in loop; recommended answers auto-accepted per dispatch override). Upstream locks from the 2026-07-03 source-spec grill (P3 deterministic per-(theme×fund) chips with ±0.15 bands + absence≠zero, P4 strength-tag trio, P5 required-optional mechanism via prompt 2→3 + sanitizer + LLM-explains-never-scores, NO second schema bump under "7", NO `_ENGINE_VERSION` change, per-claim LLM direction out of scope) were NOT reopened; the grill hardened the item spec's own mechanisms against CONTEXT.md, ADR 0017/0021, evals/README.md, and actual code.

Questions resolved: 12 (RD-1 … RD-12 in the spec's `## Resolved decisions`). 1 real defect fixed (RD-5: the `mechanism_validity` metric must reproduce the validity predicate verbatim — importing it from `narrative_macro` would transitively import the LLM gateway and breach the ADR 0017 §3.3 scorer-purity ban; the module's own `_BANNED_VERBS` verbatim-copy precedent settles the pattern), 4 gaps pinned (RD-3 parser shape-dispatch + claims-driven block emission, RD-7 strength tag on the `idx=None` render path, RD-8 `format_signed` negative zero, RD-9 sanitize-drop compares the stripped candidate), 1 stale-doc adoption (RD-11: `docs/monitor/README.md` still says schema 6 twice — 001 landed "7"), 6 verifications-with-evidence (RD-1 join key unvalidated at `impact_validate.py:37` + first-wins = LLM emission order; RD-2 `unknown ∈ _VALID_STRENGTH` at `narrative_macro.py:22`, no render path shows strength today, 归因未知 is honest-unknown not invented confidence; RD-4 hardcoded `"2"` at `monitor_cmd.py:485`, no test pins it; RD-6 phone-first compliant — all load-bearing info inline, hover carries only locked-enhancement confidence; RD-10 corpus/loader/threshold conventions hold, double-gate untouched; RD-12 bundles in scope at `monitor_cmd.py:1050`, reconciliation-by-construction holds). Zero unresolved.

Docs touched:
- `CONTEXT.md` — new term **Mechanism clause (传导线) / macro direction chips** added to the Monitor vertical section (after 宏观面速览): deterministic direction chips (rendered values = trace impacts, absence ≠ zero, exact-string best-effort join, first-wins), required-optional mechanism semantics (drop-never-truncate, no retry consumed), LLM-explains-never-scores boundary, and the 归因未知 fourth tag. No other entries touched (no bloat).
- `docs/adr/` — **no new ADR** (RD-10: three-of-three fails for every candidate; ADR 0021 checked for a prompt-version policy — has none; the 001 grill's no-ADR precedent for the schema-7 carrier covers the additive `mechanism` trace field; trace.py:16 explicitly anticipates additive fields under "7").
- `docs/2026-07-03-monitor-v4-explainability/items/002-spec.md` — refined in place (criteria 1, 4, 9, 13, 14; appended `## Resolved decisions` RD-1..RD-12). Strike-through preserved where a phrase was corrected (AC13); nothing deleted.

Spec refined: yes — the load-bearing changes are RD-5 (purity-ban defect), RD-3 (retry-boundary shape dispatch), RD-7/RD-8/RD-9 (render/format/sanitize pins), RD-11 (adopted one-word doc repairs). Full rationale + code citations live in the spec's `## Resolved decisions`.
