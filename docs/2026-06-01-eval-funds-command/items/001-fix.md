# Triage + fix — item 001

Post-ship fix rounds: **0** (loop exit contract met on first pass).

## Loop exit contract — all three post-ship verdicts satisfy

| Verdict | File | Result |
|---------|------|--------|
| Verify (non-web XOR) | `items/001-verify.md` | **PASS** — real CLI run on 10 cached ids; md+json written; honesty invariant confirmed live |
| Review (inline /ship 8+9) | `items/001-review.md` | **PASS-WITH-NITS** — zero blocker/latent bugs |
| PR review (/code-review #91) | `items/001-pr-review.md` | **PASS-WITH-NITS** — zero new high-confidence bugs |

## Why 0 post-ship rounds

The 4 latent edge bugs surfaced by `/ship` steps 8+9 were fixed **pre-push** (commit
`9ad77a2`, the sanctioned "ship review demands fixes before push" path), so the PR opened
already clean. No FAIL verdict landed post-ship → no fix round required.

## Findings triaged as NON-blocking (nits / deferred)

Nits (accepted — cosmetic, do not block per autodev contract):
- `fund_eval.py:87` `_sort_key` redundant first element — defensive, harmless (reviewer confidence 52).
- `fund_eval_cmd.py:23` `--ids` silently ignored when `--ids-file` also given — spec §3.1 frames the two as "one of the two required" (providing both is undefined); current precedence (ids_file wins) is reasonable. Low value; not changing behavior.
- `test_fund_eval.py:108` uses `type(inp)(**{**inp.__dict__, ...})` — this constructs a NEW instance (immutability-respecting), not an in-place mutation; reviewer flag is borderline. Left as-is.

Pre-existing (NOT introduced by this PR — filed to `TODOS.md` → Reliability):
- `states.py` `derive_thesis_from_evidence` ActiveFundSnapshot branch lacks the dual-leg (data+information) coverage check the FundLevelSnapshot branch has.
- `fundamentals/snapshot_cache.py` `load_active_fund_cache` swallows `OSError`/`ValueError` without logging.

These are surfaced to the user in the final summary; the command reuses the pipeline
classifier verbatim by design (spec §1), so the dual-leg gap is out of scope for this item.
