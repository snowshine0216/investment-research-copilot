# SKIPPED — `irc narrative` Thematic Fund Mining

No items skipped (single-feature spec mode, N=1).

## Forward notes (not blocked work — explicit follow-ups per spec)

- **`ai` / `robots` narrative configs** — spec §7 states these are added later using the same schema with **no code change** (just new `config/narratives/<name>.yaml` files). Out of band for this run; they validate the reusability claim once the code lands.
- **`compute_metals` basket approval** — seeded as a draft this run, flagged in the PR for user approval before being treated as frozen.
- **Universe expansion** (`universe build-cn-funds`) — the screen runs over the curated universe only; whole-market screening is a non-goal (spec §2).
