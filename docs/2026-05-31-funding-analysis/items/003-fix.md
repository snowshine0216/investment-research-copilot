# 003 — triage-fix loop (post-ship)

The post-ship verifiers surfaced findings beyond the pre-push ship-review fixes. Two rounds of fixes; loop now exits clean (all 3 post-ship verdicts PASS / PASS-WITH-NITS, zero blockers/latent).

## Round 1 — BJ-exchange latent bug (from pr-review round 1)
- **Finding:** `tushare_provider.py::_to_ts_code` mapped Beijing-exchange codes (`4xxxxx`/`8xxxxx`) to `.SZ` instead of `.BJ` → Tushare received the wrong ts_code → empty data for BJ constituents. Tushare-only, token-gated, degraded to None (no crash), but a real correctness defect.
- **Fix:** commit `3ddbb3c` — `_to_ts_code` now mirrors the canonical `_parse_exchange_from_ticker` convention (`5`/`6`→SH, `4`/`8`→BJ, else SZ). 4 tests added (BJ red→green).

## Round 2 — DEEPSEEK coupling regression (from pr-review round 2)
- **Finding:** `default_cn_provider()` instantiated `Settings()` (requires `DEEPSEEK_API_KEY`), so `irc fundamentals snapshot` — a fetch-only command with no LLM dependency that previously made zero `Settings()` calls — now failed without the LLM key. The Tushare token is optional; coupling the CN fetch layer to the LLM key is wrong.
- **Fix:** commit `e75b29a` — new `_read_tushare_token()` helper catches `ValidationError` and degrades to AkShare-only, decoupling the fetch layer from the DEEPSEEK key (restores the pre-003 ability to run fundamentals-only without it). Test added (red→green); `Settings` untouched.

## Round 3 — re-review confirms clean
- pr-review round 3: **PASS-WITH-NITS**. Both prior latent bugs verified RESOLVED at HEAD. One nit only — an `assert` precondition in `valuation_fundamental.py:61` (item-002 code, out of 003's diff scope) → deferred to TODOS.

## Verifier re-runs
- pr-review (the failing verifier) re-run after each fix: round 1 PASS-WITH-NITS (BJ) → round 2 PASS-WITH-NITS (DEEPSEEK) → round 3 PASS-WITH-NITS (clean, nit→TODOS).
- verify stayed PASS (the fixes are in the Tushare path / provider edge; verify's ACs unaffected).
- Full suite re-run after both fixes (final): see items/003-ship.md test notes.

## Exit
Loop exits: verify PASS + review PASS-WITH-NITS + pr-review PASS-WITH-NITS, zero blockers/latent. 2 fix rounds.
