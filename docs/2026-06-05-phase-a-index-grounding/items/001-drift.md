Verdict: PASS

Subagent: sonnet
Plan checklist items: 10
Verified present in diff: 10

Drift findings:
  - Task 1 — OK
    Evidence: src/irc/fundamentals/akshare_index_valuation.py — `_INDEX_PE_PB_NAME`/`_PE_COLS`/`_PB_COLS` removed; `_LEGULEGU_INDEX_SYMBOL`, `_SPECULATIVE_LEGULEGU_SYMBOL`, `_LEGULEGU_PE_TTM_COL`, `_LEGULEGU_PB_COL` added; both fetch functions updated to single-candidate tuples; module docstring updated; `from irc.opportunity.lookthrough import _BROAD_INDEX_DISPLAY` import deleted. All seven new D1/D2 tests present; three existing broad-fetch tests updated to `_PROD_PE_FRAME`. Helper-level tests (`_PE_FRAME` / `_PB_FRAME`) untouched.
    Action: accepted

  - Task 2 — OK
    Evidence: src/irc/data/index_valuation_ingestor.py — `replace_keys: bool = False` param added; `keys_to_replace` list and per-key DELETE loop added; `if hist is None or not hist.rows` guard added; early `return 0` when `params` is empty. All three new tests present in tests/data/test_index_valuation_ingestor.py.
    Action: accepted

  - Task 3 — OK
    Evidence: src/irc/commands/ingest_cmd.py — import of `_LEGULEGU_INDEX_SYMBOL` added, `_BROAD_INDEX_KEYS` import removed; broad leg changed from `tuple(sorted(_BROAD_INDEX_KEYS))` to `tuple(sorted(_LEGULEGU_INDEX_SYMBOL))` with `replace_keys=True`. New unit test present in tests/commands/test_ingest_cmd.py. Note: the unit test as written (per the plan's Step 1 note) only asserts the allowlist literal values and does not monkey-patch `run_ingest` — the plan explicitly acknowledged this limitation ("This test pins the allowlist literal") and accepted it.
    Action: accepted

  - Task 4 — OK
    Evidence: src/irc/opportunity/lookthrough.py — `chinext50` added to `_BROAD_INDEX_DISPLAY`; `chinext` corrected to `创业板指`; `csi_dividend_lc` corrected to `中证红利低波`; `_INDEX_NAME_TO_SLUG` extended with `**{name.lower(): slug for slug, name in _BROAD_INDEX_DISPLAY.items()}`. Four new tests present in tests/opportunity/test_lookthrough.py.
    Action: accepted

  - Task 5 — OK
    Evidence: tests/opportunity/test_inputs_loader.py — three new tests present (`test_broad_display_name_tracked_index_grounds_pe_percentile`, `test_sp_dividend_low_vol_tracked_index_stays_none`, `test_speculative_slug_with_empty_table_stays_none`). No source change (correct — Task 4 wired the inversion). Incidental: one stale `from irc.opportunity import inputs_loader` import removed (ruff cleanup, incidental).
    Action: accepted

  - Task 6 — OK
    Evidence: config/universe/cn_funds.yaml — `161721` and `003318` seed entries added WITHOUT `tracked_index`. src/irc/templates/config/universe/cn_funds.yaml — same two entries mirrored (plan Task 6 Step 5). tests/test_config_loader.py — `test_phase_a_seed_overrides_strip_broad_tracked_index` added, asserts `161721.tracked_index is None`, `003318.tracked_index is None`, `023153.tracked_index == "中证A500"`.
    Action: accepted

  - Task 7 — OK
    Evidence: tests/fundamentals/test_index_valuation_live.py — single-symbol `test_fetch_cn_index_valuation_csi300_live` replaced with `@pytest.mark.parametrize("slug", sorted(_LEGULEGU_INDEX_SYMBOL)) test_production_symbol_returns_rolling_pe_and_pb_live` (hard-assert loop over 4 allowlist symbols) and `test_speculative_symbol_landing_sweep_informational` (informational sweep, never fails). Imports updated for `_LEGULEGU_INDEX_SYMBOL`, `_SPECULATIVE_LEGULEGU_SYMBOL`.
    Action: accepted

  - Task 8 — OK (verification only — no source changes required)
    Evidence: plan states "no source change" for this task; none present in diff. The diff includes no unexpected test-fixup commits.
    Action: accepted

  - Task 9 — divergent (partial): before-after.md is a PLACEHOLDER, not a real artifact.
    Evidence: docs/2026-06-05-phase-a-broad-grounding/before-after.md — file present but begins "STATUS: PENDING LIVE CAPTURE (operator gate #5)". build_diff.py is present and matches the plan spec exactly. Plan Task 9 Steps 1–4 are explicitly operator gates requiring live cached data + LLM (unavailable in automated sandbox). The plan marks Steps 1, 2, 4 as "(operator step)" and Step 5 says "Commit the artifact." The impl agent committed the build_diff.py script and a documented placeholder in lieu of live data. This constitutes a legitimate deferred-operator-gate — not an implementation failure. Plan Task 9 Step 5 text ("do NOT commit the scratch dirs") is complied with.
    Action: accepted — placeholder is the correct deliverable from an automated agent; the plan-required live data must be generated by the operator running the reproduction commands in the placeholder. No plan amendment needed (the plan explicitly marks Steps 1/2/4 as operator steps).

  - Task 10 — OK
    Evidence: CONTEXT.md — `IndexValuation` entry updated (9→10 keys, rolling PE wording, allowlist reference); `index_valuation_history` entry updated with `replace_keys=True` / broad leg / speculative map description. CHANGELOG.md — `### Added — Phase A broad-index PE-TTM grounding` bullet added under `[Unreleased]` (no VERSION bump). docs/ROADMAP.md — Phase A row changed from `☐ open` to `✅ done (2026-06-05)` with `+9 funds` measured reach; top-line status updated; `### Phase A` heading updated to `✅ DONE (2026-06-05)`. docs/adr/ is NOT modified (confirmed from diff file list).
    Action: accepted

  - Task 4 Step 6 / snapshot.py — pre-approved outside-§7 touch: accepted — evidence below
    Evidence: src/irc/fundamentals/snapshot.py — `"创业板"` renamed to `"创业板指"` (display correction, same code 399006); `"创业板50"` added (code 399673); `"红利低波"` renamed to `"中证红利低波"` (display correction, same code 930740). No unrelated registry entries altered.
    Action: accepted

Deviation (a) test_lookthrough_sector_keys.py: legitimate-update
  Evidence: diff hunk shows the old test was `test_index_name_to_slug_excludes_broad_names` asserting `"沪深300" not in _INDEX_NAME_TO_SLUG` and `"中证1000" not in _INDEX_NAME_TO_SLUG`. The new test `test_index_name_to_slug_includes_broad_names` asserts `_INDEX_NAME_TO_SLUG["沪深300"] == "csi300"` and `_INDEX_NAME_TO_SLUG["中证1000"] == "csi1000"`. The OLD test encoded BREAK 1 exactly (the spec's name for the bug this plan fixes — broad names not inverted). The new test asserts the spec'd D3/D4 post-fix behavior. No meaningful invariant was weakened; the update inverts the assertion to match the newly correct behavior. Verdict: legitimate test update.

Deviation (b) snapshot.py _TARGET_REGISTRY: accepted
  Evidence: four diff lines changed. `"创业板"` → `"创业板指"` (rename, same code `399006` — plausible SZSE Growth Enterprise Market Index code, standard). New entry `"创业板50"` with code `399673` — plausible (SZSE ChiNext 50 Index code, cross-checked against plan comment "创业板50指数"). `"红利低波"` → `"中证红利低波"` (rename, same code `930740` — plausible CSIndex dividend low-volatility code). No unrelated entries altered. All codes are plausible standard csindex/SZSE codes, none obviously invented. The change was triggered by Task 4 Step 6's conditional (invariant test required the display names match). Accepted; no dubious code requiring pr-review flagging.
