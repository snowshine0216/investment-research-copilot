Fix rounds: 1 (proactive nit-hardening only — no blockers existed)

## Loop exit contract — SATISFIED on round 0 (no blockers)
- Verify: **PASS** (items/001-verify.md) — all 4 acceptance criteria observed against real report.json/details.json.
- Review (inline /ship steps 8+9): **PASS-WITH-NITS** (items/001-review.md) — 0 P0, 0 latent, adversarial CLEAN.
- PR review (/code-review): **PASS-WITH-NITS** (items/001-pr-review.md) — 0 blockers, independent verdict matches.

All three satisfy the exit contract with zero blockers / zero latent bugs / zero high-confidence PR-review bugs. The triage table classifies every surfaced finding as a **nit**.

## Triage of the nits (all flagged on a proven-unreachable path)
1. **D3 structural-dependency comment** (silent-failure-hunter) → **FIXED inline.** Added a comment at the `worst_status` aggregation point in `evals/monitor_forward/runner.py` documenting that `engine_population` can only WARN when the headline is already WARN, so it never gates — and to re-check D3 before any future trigger change. The invariant was previously only in the spec; this guards a future threshold edit. Pure comment, zero behavior change.
2. **Defensive assertion `assert 0 <= n_excluded_engine <= n_total_raw`** (code-reviewer, silent-failure-hunter, /code-review — flagged 3×) → **ACCEPTED, not implemented.** Rationale: the adversarial reviewer *proved* this path unreachable (`engine_mismatch` is counted from the same `ledger` that `n_total_raw = len(ledger)` measures, so `n_excluded_engine <= n_total_raw` always holds). Adding an assertion that cannot fire is untestable production code — incompatible with the project's TDD mandate (no failing test can be written) and the no-dead-code lean. The D3 comment + the existing structural bound cover the intent.
3. **Test robustness** (loose `rc in (EVAL_RC_WARN, 0)`; maturity NAV-coverage) → **ACCEPTED.** Both tests currently green and the verify subagent independently exercised all 3 scenarios end-to-end; precision is adequate.

## Re-verification after the comment
- The only change since the verdicts was a pure code comment (no behavior change), so the Verify/PR-review PASS evidence remains valid.
- Sanity re-run: `tests/evals/test_monitor_forward_runner.py` + `test_monitor_forward_metrics.py` → 37 passed; `ruff check evals/monitor_forward/runner.py` → clean.

Verdict: PASS — loop exit contract satisfied; one nit fixed inline, two accepted with rationale.
