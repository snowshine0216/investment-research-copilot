# 001 — QA verdict

- **PR:** #54
- **Date:** 2026-05-21
- **Verdict:** PASS

## Acceptance criteria

| # | Criterion | Verdict | Evidence (command + tail of output) |
|---|-----------|---------|-------------------------------------|
| 1 | 270023 in YAML as qdii_global | PASS | `grep -c "instrument_id: '270023'" config/universe/cn_funds.generated.yaml` → `1`; `grep -B1 -A6` shows `asset_class: qdii_global` |
| 2 | counts block includes qdii_global | PASS | graceful-failure probe stdout includes `qdii_global/none: 16` (and consumer, healthcare, tech sub-buckets); live `build-cn-funds` confirmed via monkeypatched run |
| 3 | integration test passes | PASS | `pytest tests/integration/test_generated_cn_fund_discovery.py -v` → `2 passed in 2.98s` |
| 4 | `_apply_caps` returns={} backward-compat | PASS | adversarial probe sorts `['A','B','C']` ascending by fund_code when returns is `{}`; `test_candidate_rank_with_returns_falls_back_to_fund_code_when_empty` PASSED |
| 5 | fetch_open_fund_ranks lru-cached + graceful failure | PASS | `fetch_open_fund_ranks.cache_info()` confirms `maxsize=1`; monkeypatch raises RuntimeError → stderr `WARN: fund-rank fetch failed …; falling back to fund_code rank` → exit 0 |
| 6 | no downstream regression (3 pre-existing) | PASS | Full suite: `3 failed, 1490 passed, 17 skipped`; same 3 tests fail identically on `main` (`test_thesis_coverage_meets_threshold`, `test_no_all_evidence_insufficient_valuation`, `test_eval_single_stage_data`) |

## Adversarial probes

- **Empty-returns rank backward-compat:** `_candidate_rank_with_returns({})` called on funds `['A','B','C']` — sorted output `['A', 'B', 'C']` (fund_code ascending). Matches pre-PR tiebreaker. PASS.
- **Graceful akshare failure path:** Monkeypatched `fetch_open_fund_ranks` to `raise RuntimeError('simulated akshare failure')`. `run_build_cn_funds(tmpdir)` printed `WARN: fund-rank fetch failed (simulated akshare failure); falling back to fund_code rank` to stderr, produced 388-instrument YAML, and returned without raising (exit 0). PASS.
- **Pre-existing failures confirmed on main:** Ran `pytest tests/integration/test_thesis_coverage.py tests/test_e2e_full_pipeline.py -v` on `main` branch. Got exactly 3 failures: `test_thesis_coverage_meets_threshold` (34/86 instruments evidence_insufficient), `test_no_all_evidence_insufficient_valuation` (same), `test_eval_single_stage_data` (exit code 2 instead of 0/1). Same errors appear on PR branch. Not a regression.

## Issues found

(none)

## Notes

- 13 commits on `autodev/universe-quality-ranking-qdii-global` vs `main`, all matching the 11-task plan (plus ship-verdict and version-bump commits).
- Focused test suite (discovery, akshare_client, schemas, universe_cmd): 217 passed in 2.89s.
- Full suite excluding pre-existing failures: 1490 passed.
- The `qdii_global` bucket appears in 4 sub-buckets in the live build: `consumer: 2`, `healthcare: 8`, `none: 16`, `tech: 4` (total 30 instruments), confirming the classification is active at the correct scale.
- `fetch_open_fund_ranks` is wrapped with `@lru_cache(maxsize=1)` — confirmed via `cache_info()` attribute presence.
