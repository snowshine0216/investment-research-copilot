Verdict: PASS

Body:
Subagent: sonnet
Source: Fallback used: direct entry-point protocol (invoked Skill(skill="verify") first; its guidance — build a handle, drive the real surface, no test/typecheck substitution — was applied manually via the protocol given in the dispatch prompt, since this repo has no matching `verifier-*`/`run-*` skill under `.claude/skills/`)
Entry point exercised: `uv run --project /Users/snow/Documents/Repository/investment-research-copilot irc monitor`, run from cwd `/private/tmp/claude-501/.../scratchpad/verify-001` (isolated scratch dir, scaffolded via `irc init` from the same `--project`). No `.env`, no API-key env vars set (confirmed via `env | grep -iE 'deepseek|minimax|openrouter|jina|irc_'` → empty, both before and after the run).

Observed behavior:
- rc == 0 — confirmed via captured `echo "RC=$?"` after the run: `RC=0`. The run survived a real `ReadTimeout` against `api.minimaxi.com` (ADR 0017 MiniMax route, no key, timed out through a system-level proxy at 10.27.7.110:8080) without crashing — this is the P0-class degradation contract in action, not a mocked path.
- report.html exists (`outputs/2026-07-02/monitor/report.html`, 650971 bytes, well-formed — ends `</ol></details></body></html>`, no truncation).
- 今日速览 section — `<h2>今日速览</h2>` present, with a rendered `数据健康` row (overview strip Comp 5, spec §7).
- Header provenance — literal substring `engine 3 · prompt 2 · schema 6 ·` present in the header line (engine version unchanged from v2, per spec §2 non-goal).
- Validation panel vocabulary — `观测` appears exactly 2× (the two informational-only stages, `flow_coverage`/`valuation_coverage`, spec §8), while literal `PASS` appears only on gate-relevant rows (`monitor_signal`, `deterministic_scoring`, reconciliation rows) — the informational/gate vocabulary split holds.
- 宏观面 macro section — honestly absent, not a crash or empty shell: `eval_trace.json["macro_narrative"]` = `{"status": "provider_error: deadline 60.0s exceeded after 1 attempts", "blocks": []}` (key exists, additive, degraded value); `narrative.json["__macro__"]` mirrors the same degraded status; the report has zero `id="macro-*"` anchor targets (grep-verified: 0 anchors for 8 distinct theme-chip hrefs), so all per-fund `theme-chip` links degrade to dangling anchors rather than the report failing to render — exactly the "absent section, never a crashed run" contract in spec §10.
- Appendix — present (`Evidence` heading, `<ol>...<li id="ev-...">` entries, 37 deduped citation `<li>` elements, closes cleanly). Not honestly-absent in this run because theme evidence was fetched successfully (no LLM needed for evidence gathering, only for narrative synthesis) — this is expected: evidence pool and macro narrative are separate degradation surfaces per spec §10's data-flow diagram.
- No `<script` tags (grep count 0). No literal `基金概况` (grep count 0).
- eval_trace.json: `schema_version` == 6 (confirmed via `json.load` — not a string-grep false positive). `macro_narrative` present as a top-level key per the additive schema-5→6 bump (spec §2/§5 consequence).
- Forward-ledger isolation — scratch dir `data/monitor/forward_ledger.jsonl` gained 10 fresh rows (one per monitor-set fund) from this run; the **real repo's** `data/monitor/forward_ledger.jsonl` mtime is unchanged (Jul 2 12:29, predating this session) and `git status --short` in the real repo is empty — confirms zero mutation of operator state.

Render-level acceptance criteria checked statically in the produced HTML (spec §11):
- Tier badge CSS class present and populated: `.tier-badge{color:#57606a;font-size:11px}` in the `<style>` block, and 3 of the 4 badge values actually rendered in the appendix (`未分级`×11, `权威`×10, `财经媒体`×16 — `快照` absent this run, consistent with no constituent-pool citations surfacing in the appendix on this particular run).
- Overview-strip CSS rule present: `.overview-row{margin:4px 0}`.
- Timeline `名称(代码)` format verified structurally (not just substring match) by parsing the `<table class="timeline-table">` and confirming all 10 monitor-set fund rows render as `名称(代码)` (e.g. `汇添富消费行业混合(000083)`, `广发全球精选股票(QDII)人民币A(270023)`) — zero bare 6-digit-only rows found.

Findings (not gating, but worth flagging):
- The `irc init` config **template** (`src/irc/templates/config/llm.yaml` and `src/irc/templates/config/spend_pricing.yaml`) is missing the `minimax` provider block and the `monitor_impact`/`monitor_narrative` task routes/pricing/seeds that ADR 0017 requires. A fresh scratch install crashes at the spend-preflight `KeyError: 'monitor_impact'` (then `KeyError: 'minimax'`) before ever reaching `irc monitor`'s actual pipeline body. **This predates item 001** — reproduced identically on `main` (`1876987c`) via a disposable `git worktree`, byte-identical traceback — so it is not a regression introduced by this branch's diff (confirmed empty `git diff main...HEAD -- src/irc/spend/ src/irc/templates/config/llm.yaml`). I worked around it by hand-patching only the scratch dir's copies of `config/llm.yaml` and `config/spend_pricing.yaml` (adding the same `minimax` block the real repo's own hand-maintained config already has) — no secrets, no keys, just routing/pricing config — in order to reach the real target of this verification (the report renderer). Flagging as an out-of-scope pre-existing gap; the real operator's `config/llm.yaml` already has these entries by hand, so it never surfaces in normal use, but it does mean a *brand-new* `irc init` install cannot run `irc monitor` at all until this template gap is fixed.

Failures: none (relative to item 001's own diff and acceptance criteria). The one crash observed traces to a pre-existing template gap outside item 001's changed files, not to this item's implementation.
