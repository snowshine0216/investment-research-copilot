# 001 fix log

Two fix rounds; all three post-ship verdicts now PASS / PASS-WITH-NITS with no blockers or latent
bugs outstanding.

## Round 1 — pre-push (during /ship steps 8+9; see items/001-ship-blocked.md)
Blocker + 5 real bugs in new M0 code, fixed BEFORE the PR was opened. Commits c095f74..e8750b2.
- test_monitor_constituent.py 2-tuple unpack (blocker) · latest_report corrupt-file guard ·
  resolve_health naive-tz TypeError · nav_quality date.today() purity · panel hardcoded PASS ·
  latest_per_key KeyError hardening. Re-verified: 135 passed, ruff clean.

## Round 2 — post-ship nits (from /code-review, items/001-pr-review.md)
Both nits were non-blocking (exit contract already satisfied) but trivial + quality-positive, so
polished inline:
- render_html.py — moved `StageHealth` import from a deferred local in `_panel()` to module level
  (already imports `GateDecision` from the same module; no cycle introduced — import smoke OK).
- monitor_cmd.py — `_write_outputs` `gates: tuple = ()` → `gates: tuple[GateDecision, ...] = ()`.
Verified: ruff clean, imports OK, 16 focused tests pass, DAG test unchanged (its failure is
pre-existing on base, not a new cycle).

## Exit state
- /verify: PASS
- review (/ship 8+9 inline): PASS
- pr-review (/code-review): PASS-WITH-NITS → nits now fixed
No blockers, no latent bugs. Ready for merge.
