# Item 002 — WS-2 Macro direction + mechanism (P3 + P4 + P5)

**Run:** monitor-v4-explainability · **Source spec:** [2026-07-03-monitor-report-v4-explainability-design.md](../../superpowers/specs/2026-07-03-monitor-report-v4-explainability-design.md) §1 problem 2, §2 P3/P4/P5, §3 WS-2, §4 bullet 2, §5 out-of-scope
**Schema:** the eval-trace bump 6→7 ALREADY LANDED with item 001 (merged #201) — this item adds its per-theme `mechanism` field **under the existing `"7"`** and must NOT bump again. **Narrative prompt version 2→3 lands with THIS item.**

## Goal

Make the 宏观面速览 answer "positive/negative for WHICH fund, and WHY" instead of listing neutral bullets. Today `render_html._macro_theme_section` renders each theme's fund chips as bare `<span class="fund-chip">` (no direction) and `_macro_claim_html` drops `attribution_strength`, even though every fund's validated `impacts["macro"]` records `{key, impact −1..+1, confidence, citation_ids}` already sit in the trace (`trace._impacts`, from `FundTraceBundle.macro_impacts`) and each claim already carries a strength value. This item: (1) **P3** — a new pure join `(macro_narrative blocks × per-fund macro impacts)` colors each theme's fund chips and inlines the signed impact value (`008986 +0.8`; green ≥ +0.15 / red ≤ −0.15 / grey between; a fund with NO record for the theme stays uncolored with no number — absence ≠ zero; confidence trace-only, `title` attr as progressive enhancement; one legend line), with **no new LLM output for direction**; (2) **P4** — each claim bullet gets its strength tag (可能主因 / 方向一致 / 已证实归因), renderer-only; (3) **P5** — the ONE run-global `monitor_narrative` call (prompt 2→3, `narrative_macro._build_macro_messages`) additionally emits one ≤60-char mechanism clause per theme (e.g. 就业数据疲软→加息预期降温→利多黄金/成长), validated required-optional (missing/oversized/imperative-bearing → the theme renders WITHOUT it, never fails the block), rendered under the theme header as `对本组基金的传导：…`, persisted additively in the trace under schema `"7"`, and covered by an extension of the `monitor_narrative` live-eval corpus. The LLM explains, never scores — direction stays deterministic from impacts.

## Acceptance criteria

Each bullet is independently verifiable by the named test file or command.

**Slice 1 — pure direction join (P3 core)**

1. New pure module `src/irc/monitor/macro_direction.py` (mirror test `tests/monitor/test_macro_direction.py`; no I/O, no clock, < 200 lines) exposing:
   - `join_macro_impacts(macro_impacts_by_fund: dict[str, tuple[ValidatedImpact, ...]]) -> dict[str, dict[str, ValidatedImpact]]` — theme → fund_id → record, joined on **exact string equality** `ValidatedImpact.key == theme`; duplicate keys for the same fund resolve **first-wins** (deterministic — input tuples preserve LLM emission order); empty input → `{}`.
   - `direction_class(impact: float) -> str` — `"chip-pos"` iff `impact >= 0.15`, `"chip-neg"` iff `impact <= -0.15`, else `"chip-flat"`. Boundary tests at exactly `+0.15` (green) and `-0.15` (red).
   - `format_signed(value: float) -> str` — `f"{value:+.2f}"` with trailing zeros then a trailing `.` stripped (`+0.80`→`+0.8`, `+0.85`→`+0.85`, `+1.00`→`+1`, `0.0`→`+0`, `-0.15`→`-0.15`). Negative zero normalizes: `value == 0.0` (True for `-0.0`) short-circuits to `"+0"` — `f"{-0.0:+.2f}"` would otherwise render a nonsense `-0` chip (RD-8; boundary test included).
   Unit tests additionally cover: fund with no record for a theme absent from the join (downstream chip stays uncolored/no number), impact key matching no rendered theme is simply never looked up (tolerated, stays trace-only).

**Slice 2 — renderer (P3 + P4 surfaces)**

2. `render_html._macro_theme_section`: a fund chip WITH a joined record renders `<span class="fund-chip {direction_class}" title="置信度 {confidence}">{fund_id} {format_signed(impact)}</span>` (confidence formatted with the same trim rule, unsigned); a fund chip WITHOUT a record renders exactly as today — `<span class="fund-chip">{fund_id}</span>`, no color class, no number, no title. Chip set and order remain `fund_themes_by_theme` (config-derived, `_invert_fund_themes`) — the renderer NEVER invents a chip for an impact key outside the config chip list. Tests in `tests/monitor/test_render_html.py`: colored+valued chip, uncolored absent-record chip, no-invented-chips, title attr present iff record present, all text HTML-escaped.
3. One legend line renders ONCE inside the 宏观面速览 section (immediately after the `<h2>`, before the first theme block), exact text: `<p class="macro-legend">图例：数值 = 该主题对基金的影响（−1 利空 … +1 利多）；绿 ≥ +0.15 · 红 ≤ −0.15 · 灰 = 其间；无数值 = 当日无该主题影响记录</p>`. Absent when the section itself doesn't render (empty/degraded doc — unchanged early-return).
4. `render_html._macro_claim_html`: every claim bullet is prefixed by `<span class="claim-strength">{label}</span>` with the locked map `possible_driver → 可能主因`, `consistent_with → 方向一致`, `supported_attribution → 已证实归因`, and the fourth validator-legal value `unknown → 归因未知` (see resolved Q5). The tag applies on BOTH claim render paths: `_macro_theme_section`'s current `idx is None` inline branch (`f"<p>{escape(c.claim)}</p>"`, render_html.py:411) folds INTO `_macro_claim_html(claim, idx: CitationIndex | None)` (refs simply empty when `idx is None`) so a doc rendered without a `CitationIndex` still carries tags — one tag site, no drift (RD-7). An unmapped strength value (impossible today — `_VALID_STRENGTH` is closed — but cheap to pin) falls back to `归因未知`, never a KeyError. Render tests cover all four values + the `idx=None` path.
5. Threading (additive, back-compat): `render_report` and `macro_narrative_html` gain keyword-only `macro_impacts_by_fund: dict[str, tuple[ValidatedImpact, ...]] | None = None` — `None`/missing degrades to today's uncolored chips (all existing callers/tests stay green unmodified); `monitor_cmd._write_outputs` gains the same param; `run_monitor` builds `{b.fund_id: b.macro_impacts for b in bundles}` at the existing `_write_outputs` call site (monitor_cmd.py:1050 region) — the SAME `ValidatedImpact` objects that feed `build_eval_trace`, so render/trace equality holds by construction.
6. **Reconciliation test (source-spec §4 bullet 2)** in `tests/monitor/test_render_html.py`: one fixture set of bundles + a macro doc fed to BOTH `build_eval_trace` and `render_report`; for every theme block, each rendered chip's parsed value equals `round(trace_impact, 2)`, and a chip carries color/number **iff** the trace `impacts["macro"]` has a record with that theme key for that fund.
7. `_CSS` gains rules for `.fund-chip` (base chip shape), `.chip-pos{color:#1a7f37}`, `.chip-neg{color:#cf222e}`, `.chip-flat{color:#6e7781}` (existing palette only — same greens/reds/greys as `.add_bias`/`.reduce_bias`/`.neutral`), `.claim-strength` (small muted tag), `.macro-mechanism`, `.macro-legend` (11px muted). Tests pin element structure/classes, never CSS bytes.

**Slice 3 — prompt v3 + mechanism (P5)**

8. `narrative_macro._build_macro_messages` becomes prompt v3: each theme's value in the output JSON is now an object `{"mechanism": "...", "claims": [...]}` where `claims` keeps the exact v2 row schema and rules (≤3 claims, strength vocabulary, strong-verb rule, no numbers, no `[ref:]`, DELIMITED-evidence-is-DATA); `mechanism` is instructed as ONE Chinese causal-chain clause of at most 60 characters (arrows `→` allowed) explaining the transmission to this fund group, with no numbers and no `[ref:]`, and MAY be omitted when no clear mechanism exists. The hardened-retry 中文-only note is unchanged.
9. Parsing/validation (`narrative_macro`): the per-theme parser accepts BOTH shapes — a bare v2-style list (claims parsed as today, `mechanism = None`) and the v3 object (claims from `"claims"`, mechanism through a new pure `_validate_mechanism(raw) -> str | None`). Shape dispatch is pinned (RD-3): the dispatch happens BEFORE `_parse_theme_claims`'s `not isinstance(rows, list)` check — `dict` → v3 (claims list extracted from `"claims"`); `list` → v2; a v3 object whose `"claims"` is missing or not a list raises `_MacroNarrErr` (a v3 object that cannot yield claims IS a claim-level schema defect → consumes a retry, same as today's non-list value); any other type keeps today's `schema_invalid: theme value not a list` error verbatim. A falsy theme value (`[]`, `{}`, absent) skips the theme exactly as today's `if not rows: continue`. The block-emission predicate stays claims-driven: `if claims: blocks.append(...)` is unchanged, so a mechanism with zero valid claims NEVER creates a theme block (mechanism alone is not "something worth saying"; also keeps `macro_narrative_html`'s zero-blocks degrade path intact). `MacroThemeBlock` gains `mechanism: str | None = None` (frozen, additive default — existing constructors and fixtures unaffected). `_validate_mechanism` NEVER raises; it returns `None` (theme renders without the line, block never fails, no schema-retry consumed) when the value is: not a `str`; empty/whitespace after `.strip()`; longer than `_MAX_MECHANISM_CHARS = 60` code points after strip (drop, never truncate); changed by `sanitize_untrusted` (imperative/injection-bearing); or failing `_passes_language_guard`. The sanitize comparison runs on the STRIPPED candidate (`sanitize_untrusted(stripped) != stripped ⇒ drop`) — `sanitize_untrusted` itself ends with `.strip()` (evidence.py:97), so comparing against the raw value would false-drop a benign whitespace-padded mechanism (RD-9). Claim-level errors keep today's `_MacroNarrErr` schema-retry behavior byte-for-byte. Unit tests in `tests/monitor/test_narrative_macro.py`: valid mechanism kept verbatim, each drop reason → `None` with claims intact, bare-list shape → `None`, oversized dropped not truncated, injection-bearing dropped, whitespace-padded benign mechanism kept (stripped), v3 object with invalid claims still raises (retry loop unchanged), v3 object with missing/non-list `"claims"` raises, mechanism-only theme (empty claims) emits no block, 60-char boundary kept / 61 dropped.
10. `narrative_macro.PROMPT_VERSION = "3"` module constant; `monitor_cmd` Provenance construction consumes it (the hardcoded `"2"` literal at the `Provenance(_ENGINE_VERSION, "2", SCHEMA_VERSION, "")` site is gone — same anti-drift move as item 001's `SCHEMA_VERSION` unification); the report header renders `prompt 3`.
11. Trace: `trace._macro_narrative` block dicts gain `"mechanism": b.mechanism` (str | None). `SCHEMA_VERSION` remains `"7"` — a test in `tests/monitor/eval/test_trace.py` pins BOTH the new field and the unchanged `"7"` (the no-second-bump cross-cutting rule). `monitor_cmd._narrative_dump`'s `__macro__` block dump gains the same additive key (write-only debug artifact, verified reader-free).
12. Render: when `block.mechanism` is set, `_macro_theme_section` renders `<p class="macro-mechanism">对本组基金的传导：{escape(mechanism)}</p>` between the fund-chips div and the claims; absent (no empty element) when `None`. Render tests: with and without; escaping pinned. Source-spec §4 bullet 2 end-to-end: a mechanism line appears when prompt-v3 output validates, and no line — never a crash — when it doesn't.
13. Live-eval corpus extension (`monitor_narrative`, stays `live_gated` behind `IRC_RUN_LIVE_LLM_EVAL` + the eval-live spend gate — no gating change):
    - Two new corpus cases `src/irc/monitor/eval/cases/narrative/mechanism_1.json` / `mechanism_2.json`, category `"mechanism"`, ordinary evidence pools on distinct themes (loader is filename-sorted; no loader change).
    - New pure metric `mechanism_validity(cases, outputs)` in `src/irc/monitor/eval/metrics_narrative.py`: over `"mechanism"`-category cases, a theme entry scores valid iff its mechanism is ABSENT (required-optional — a v2 bare-list entry or a dict without `"mechanism"` is valid) or passes the validity predicate (str, non-empty after strip, ≤60 code points, CJK guard) AND contains no digit / `[ref:]` marker. The predicate is REPRODUCED VERBATIM in `metrics_narrative.py` (small pure helpers: length/strip check + a `_cjk_ratio ≥ 0.30` copy), NOT imported from `narrative_macro` — ~~"passes the production validity predicate"~~ *importing it would transitively import `irc.llm.gateway`/`http_client` (narrative_macro.py:11-12) and breach the ADR 0017 §3.3 scorer-purity ban (no gateway import in eval cores); the module already sets this precedent with `_BANNED_VERBS = (...)  # narrative._banned_verb_present, verbatim` (metrics_narrative.py:12)* (RD-5). Miss boundary pinned: an output with NO theme entries at all (the degraded `{}` from `drive_case`) counts as a miss for that case (Finding-3 convention); a PRESENT theme entry without a mechanism is valid. `_all_claims` (and thus every existing metric) becomes shape-tolerant: dict-shaped theme values contribute `value.get("claims", [])` when that value is a list (else `[]`), list-shaped values contribute themselves.
    - `evals/monitor_narrative/runner.py` adds `("mechanism_validity", …, _MECH_TH, "higher_is_better")` with `_MECH_TH = {"fail_below": 0.80}`.
    - Pure metric tests in `tests/monitor/eval/test_metrics_narrative.py` (both shapes, absent-is-valid, digit/oversize/degraded misses); no live call in unit tests.

**Docs + hygiene**

14. `docs/monitor/README.md` (macro-block bullet ~line 69 and report-anatomy mention ~line 123) updated for direction chips + strength tags + 传导 line; while in that file, repair the two stale `eval_trace.json (schema 6)` mentions (lines 75 and 218 — item 001 landed schema `"7"` but did not sync this file; a one-word fix each, RD-11); `docs/diagrams/monitor-workflow.html` macro node/notes synced (`monitor_narrative` prompt v3, direction chips deterministic from impacts); `evals/README.md` narrative metric list gains `mechanism_validity (FAIL <0.80)`; CONTEXT.md *Mechanism clause (传导线) / macro direction chips* entry ALREADY LANDED with this item's grill (2026-07-03) in the Monitor vertical section — covering deterministic direction chips (values = trace impacts, absence ≠ zero), required-optional mechanism semantics, LLM-explains-never-scores, and the 归因未知 fourth tag; implementation only needs to keep it accurate, not add it.
15. CHANGELOG `[Unreleased]` entry added; `VERSION` NOT bumped (versioning convention).
16. `uv run ruff check src tests` clean; `tests/monitor/` and `tests/monitor/eval/` green; every test dir exercising a changed signature swept (grep callers of `render_report` / `macro_narrative_html` / `_write_outputs` / `_build_macro_messages` under `tests/` and `evals/`), with touched `tests/commands/` files run **per-file** (whole-dir hangs).
17. `_ENGINE_VERSION` untouched (`git diff` shows no change); no factor/weights/bands change anywhere in the diff; forward-ledger append logic untouched.

## Non-goals

- **Per-claim LLM direction** — locked OUT (source spec §5); direction is per (theme × fund), deterministic from existing impacts. No new/changed `monitor_impact` output, prompt, or validation.
- No confidence on the chip face (trace + `title` only); no rendering of impact records for themes/funds outside the config-derived chip list (trace keeps them).
- No second schema bump — the `mechanism` field lands under the existing `"7"`; no `_ENGINE_VERSION` bump; no change to factor math, macro_tilt, weights, or bands (`0.15` chip bands are display-only and unrelated to signal bands).
- No gate/eval-cadence changes (item 001 owns caveat surfaces + weekly refresh); no divergence detail (003); no industry fill (004).
- No truncation of oversized mechanisms (drop entirely — a clipped causal chain misleads); no retry consumed by mechanism-only defects.
- No new Chinese vocabulary beyond the locked labels, the `归因未知` fallback tag, the legend line, `置信度`, and `对本组基金的传导：` (locked in P5).
- No changes to MiniMax routing (`config/llm.yaml` `monitor_narrative: minimax` stays), spend gates, or the live-eval double-gate.

## Constraints

- **Purity / effects at edges:** the join, classifier, formatter, mechanism validator, and all render additions are pure (no clock, no I/O); the ONLY edge changes are the prompt text inside the existing `gather_macro_narrative` call and the dict built at the `_write_outputs` call site. Render purity rule from report v3 stands.
- **FP + size budget:** frozen dataclasses stay frozen (`MacroThemeBlock` gains an additive default field, no mutation); new module < 200 lines; `narrative_macro.py` (225 lines) grows modestly — extract helpers to keep functions < 20 lines; `render_html.py` (515 lines, pre-existing overage) grows minimally (chip/legend/mechanism/tag rendering only — the join lives in `macro_direction.py`).
- **Public-API stability:** all signature changes are additive keyword-only params with defaults (`render_report`, `macro_narrative_html`, `_write_outputs`); `gather_macro_narrative` / `gather_impacts` signatures unchanged; `ValidatedImpact` / `validate_impacts` unchanged.
- **TDD:** red → green per slice; test files mirror sources (`macro_direction.py` → `tests/monitor/test_macro_direction.py`, etc.).
- **Schema/prompt discipline:** exactly zero schema bumps here (pin test asserts `"7"`); exactly one prompt bump 2→3, constant-ized in `narrative_macro.PROMPT_VERSION`.
- **Security:** mechanism text, chip text, `title` attrs, tags, and legend are HTML-escaped; mechanism passes the sanitizer gate (changed ⇒ dropped) so injected imperatives can neither render nor force scores; the mechanism is display-only — it never feeds factor math, signals, or the forward ledger.
- **Perf/cost:** direction join is O(records) per run; the narrative stays ONE run-global call — mechanism adds ~60 chars × ~6 themes of completion tokens (trivial; `max_tokens=4096` untouched); two extra live-eval corpus cases only run under the double gate.
- **Known suite hazard:** `tests/commands/` must be run per-file (whole-dir hangs — pre-existing ordering issue).

## Open questions resolved during brainstorming

All resolved against actual code + the locked upstream decisions (autonomy override; P3/P4/P5 locks not reopened). None required input beyond MASTER-SPEC + source spec + code.

1. **Where the pure join lives** — a new `src/irc/monitor/macro_direction.py`, not `render_html.py` (515 lines, over budget) — one clear purpose, unit-testable without render fixtures.
2. **How impacts reach the pure renderer** — `bundles` are already in scope at the `_write_outputs` call site (monitor_cmd.py:1050 region), so a `{fund_id: bundle.macro_impacts}` dict threads through as an additive default-`None` keyword. `FundView` stays untouched (impacts are trace-domain data, not view state), and passing the same `ValidatedImpact` objects the trace serializes makes the §4 reconciliation hold by construction.
3. **Join-key reality check (dispatch asked to verify)** — the key EXISTS but is unvalidated: `validate_impacts` accepts any `key` string (`impact_validate.py:37`); theme names reach the LLM via `Themes: {…}` in the impact prompt, and `MacroThemeBlock.theme` comes from config-derived `theme_pool` keys. So the join is exact-string best-effort: non-matching keys never render (chip stays uncolored — locked absence ≠ zero — while the trace keeps the record); duplicate keys per fund resolve first-wins; the renderer never invents chips beyond the config list.
4. **Chip value format** — trimmed 2dp signed (`+0.8`, `+0.85`, `+1`), matching the locked example; the reconciliation test compares `round(trace_impact, 2)`. A genuine `impact == 0.0` record renders a grey `+0` chip — visibly distinct from an absent record (no number), preserving absence ≠ zero on the face of the report.
5. **Strength tag for `unknown`** — `_VALID_STRENGTH` legally includes `unknown` (narrative_macro.py:22), which P4's locked trio doesn't name; omitting a tag would violate §4's "strength tags on every claim" and be indistinguishable from pre-002 rendering. Resolved: `归因未知` — a statement of no assessment, not invented confidence.
6. **Prompt-v3 output shape** — per-theme object `{"mechanism", "claims"}` (option a) over a top-level `"mechanisms"` sibling (collides with the theme-name namespace and breaks `_all_claims` flattening) or a pseudo-claim row (corrupts claim parsing). The parser accepts the bare-list v2 shape as claims-only back-compat, so partial LLM compliance degrades to "no mechanism line", never to a failed block.
7. **Mechanism failures never retry** — locked "never fails the block" extended to its natural consequence: an invalid mechanism must not consume a `_MAX_SCHEMA_RETRIES` attempt either (a retry burns paid tokens to fix an optional field). `_validate_mechanism` returns `None` instead of raising; the claim-level retry loop is byte-identical to today.
8. **"Sanitizer applies" + "imperative-bearing → drop" reconciled** — claims store `sanitize_untrusted(text)` (redacted); a redacted causal chain (`[…]→利多`) would be nonsense, and P5 says the theme renders WITHOUT it. Resolved: `sanitize_untrusted(m) != m` ⇒ drop; unchanged ⇒ store as-is (which IS its sanitized form, so the sanitizer has been applied by construction).
9. **Strong-verb rule (主因/导致/由于) does not apply to the mechanism** — that rule exists to keep claim attribution honest against `attribution_strength`; the mechanism is a causal chain by design and has no strength field to justify. Injection is handled by Q8; the eval's `attribution_honesty` metric flattens claims only and is unaffected.
10. **Digits in the mechanism** — the prompt forbids them (consistent with claims), the production validator does NOT drop on digits (claims aren't validator-dropped for digits either — that's an eval concern, see CONTEXT.md `hallucination_rate`), and the new `mechanism_validity` metric counts digit/`[ref:]`-bearing mechanisms as invalid.
11. **Prompt-version literal drift** — `monitor_cmd` hardcodes `"2"` at the Provenance construction (the schema slot was unified by item 001's RD-1; the prompt slot wasn't). Resolved: `narrative_macro.PROMPT_VERSION = "3"` consumed at the call site — same anti-drift precedent, in scope because the site is already touched. No test pins the production `"2"` (verified: all render-test Provenance fixtures use `"1"`).
12. **"≤ ~60 chars" pinned** — exactly 60 Python code points on the stripped string (CJK chars count as 1; a render-width cap would be false precision).
13. **Mechanism placement** — `h3 → fund chips → mechanism → claims`: the chips ARE the "本组基金" the clause refers to, so the line reads as their caption; claims follow as evidence commentary.
14. **`mechanism_validity` threshold** — `fail_below: 0.80` (matches `_ENT_TH`): the field is required-optional and production drops invalid instances safely, so this is a quality signal, not a safety gate — the safety gates (`injection_resistance` 0.95, `citation_resolution` 1.0) are untouched.
15. **Eval-shape blast radius** — `drive_case`/`build_case_details` echo raw dicts (shape-agnostic, verified); only `metrics_narrative._all_claims` needs dual-shape tolerance, which simultaneously future-proofs every existing metric for v3 outputs replayed through the suite.

## Resolved decisions

Grill 2026-07-03 (grill-with-docs, autonomous — no user in loop; recommended answers auto-accepted per dispatch override). Upstream locks (P3 chip bands/absence≠zero, P4 strength-tag trio, P5 required-optional mechanism + prompt 2→3 + LLM-explains-never-scores, no second schema bump, no `_ENGINE_VERSION` change, per-claim direction out of scope) were NOT reopened.

**RD-1 — Join-key claim verified against code (dispatch bullet 1).**
Q: Is the theme↔impact join really exact-string and unvalidated, and is first-wins-on-duplicate the real current behavior?
A: Verified, with one precision. `validate_impacts` constructs `ValidatedImpact(str(r.get("key", "")), …)` with zero key validation (`impact_validate.py:37`); theme names reach the LLM only as prose — `f"Fund {fund_id}. Themes: {', '.join(themes)}"` (`impacts.py:42`) — so the echoed `key` is best-effort. `MacroThemeBlock.theme` comes from `theme_pool` keys (`_unique_themes(funds)`, config-derived), and the chip list from `_invert_fund_themes(views)` over config `v.themes`. There is NO current join (this item creates it), so "current behavior" for duplicates resolves to input order: `validate_impacts` preserves LLM emission order and `trace._impacts` serializes it unchanged, so first-wins over the input tuple is deterministic and means "the first record the LLM emitted" — the same record a trace reader sees first. Spec Q3/AC1 upheld unchanged.

**RD-2 — 归因未知 fourth tag verified (dispatch bullet 2).**
Q: Does `_VALID_STRENGTH` really include `unknown`; does any current render path show strength; does a 4th tag conflict with P4's locked trio or the never-overstate norm?
A: All verified. `_VALID_STRENGTH = {…, "unknown"}` (`narrative_macro.py:22`), matching the four-value `AttributionStrength` literal (CONTEXT.md); production `_parse_theme_claims` accepts it, the trace serializes it, and NO render path anywhere shows strength today (`_macro_claim_html` drops it; `render_cards.py` has zero strength usage). Source-spec §4 requires "strength tags on every claim", so a trio-only map must either crash, mislabel, or silently skip — and a skipped tag is indistinguishable from pre-002 rendering. `归因未知` is a statement of NO assessment (never invented confidence), the honest-gaps direction of the never-overstate norm — it extends the locked trio without touching its three labels. Upheld; AC4 additionally pins an unmapped-value fallback to `归因未知` (defense, unreachable today).

**RD-3 — Mechanism failures and the retry loop (dispatch bullet 3): two gaps pinned.**
Q: Does a v3 response failing mechanism validation consume schema retries, given the `_MacroNarrErr` flow and the 2026-07-03 attribution_strength hardening (#199)?
A: The spec's design (`_validate_mechanism` returns `None`, never raises) is structurally correct — only `_MacroNarrErr`/`JSONDecodeError` consume attempts (`narrative_macro.py:220`), and the hardening only added claim-level raises (`bad attribution_strength`, line 123-124). But two dispatch details were unpinned and are now in AC9: (a) the shape dispatch must run BEFORE `_parse_theme_claims`'s `not isinstance(rows, list)` raise; a v3 object with missing/non-list `"claims"` raises `_MacroNarrErr` (claim-level defect → retry, consistent with today), any other type keeps today's error; (b) the block-emission predicate stays claims-driven (`if claims:`) — a mechanism with zero valid claims never creates a theme block, so mechanism-only themes cannot resurrect the zero-blocks degrade path.

**RD-4 — PROMPT_VERSION hardcoded site verified (dispatch bullet 4).**
Q: Does the hardcoded `"2"` exist, and does anything pin it?
A: Yes: `Provenance(_ENGINE_VERSION, "2", SCHEMA_VERSION, "")` at `monitor_cmd.py:485` — the schema slot was unified by 001 (the `SCHEMA_VERSION` import + trace.py:16's "fields under \"7\" WITHOUT bumping again" comment confirm the precedent), the prompt slot was not. `grep PROMPT_VERSION src tests` → empty (constant doesn't exist yet); every render-test Provenance fixture uses `"1"`, and nothing pins the production `"2"` or the header string `prompt 2`. AC10 upheld unchanged.

**RD-5 — Eval-corpus extension (dispatch bullet 5): one real defect fixed.**
Q: Which files; does the double-gate hold; is `fail_below 0.80` conventional; can the metric use "the production validity predicate"?
A: Files verified: `load_cases` is a filename-sorted glob (`case_loader.py`) so `mechanism_1/2.json` slot in with no loader change; filename style matches the existing corpus (`attribution_honesty_1.json`). Double-gate untouched: the registry lifecycle and `eval_cmd`'s env+spend gates don't change; two more cases just extend the runner's existing loop (still the sole paid surface, ADR 0017). `_MECH_TH = {"fail_below": 0.80}` matches `_ENT_TH` verbatim (runner.py) — quality-signal tier, safety gates (1.0 / 0.95) untouched. DEFECT: the spec said the metric uses "the production validity predicate" — importing `_validate_mechanism` from `narrative_macro` would transitively import `irc.llm.gateway`/`http_client` (narrative_macro.py:11-12), breaching ADR 0017 §3.3's scorer-purity ban (eval cores import no gateway — verified: zero `irc.llm` imports under `src/irc/monitor/eval/`). Fixed in AC13: reproduce the predicate verbatim in `metrics_narrative.py`, the module's own established pattern (`_BANNED_VERBS … # narrative._banned_verb_present, verbatim`). Also pinned the miss boundary: `{}` (degraded) = miss; present entry without mechanism = valid.

**RD-6 — Chip `title` attr vs phone-first (dispatch bullet 6): compliant, no change.**
Q: Does the spec keep all load-bearing info off hover?
A: Yes. Source-spec P3 locks "the report is read on the phone, hover is not a carrier" with confidence explicitly trace-only + `title`-as-progressive-enhancement. In the item spec, everything load-bearing is inline: direction color + signed value on the chip face, the legend line, strength tags, and the mechanism line. Hover carries only confidence (locked as enhancement) and the pre-existing citation superscript tooltips. §4's "reason reachable without hover" acceptance concerns item 001's caveat surfaces, not these chips.

**RD-7 — Strength tag applied on BOTH claim render paths (new gap).**
`_macro_theme_section` has two claim paths: `_macro_claim_html(c, idx)` and an inline `idx is None` fallback (`render_html.py:411`) that would silently lose tags. AC4 now folds the fallback into `_macro_claim_html(claim, idx | None)` — one tag site, refs empty when `idx is None`.

**RD-8 — `format_signed` negative zero (new gap).**
`f"{-0.0:+.2f}"` → `"-0.00"` → trim → `"-0"`, a nonsense chip. AC1 now short-circuits `value == 0.0` (True for `-0.0`) → `"+0"`.

**RD-9 — Sanitize-drop comparison runs on the stripped candidate (precision).**
`sanitize_untrusted` ends with `.strip()` (`evidence.py:97`); comparing against the RAW value would false-drop a benign whitespace-padded mechanism. AC9 pins `sanitize_untrusted(stripped) != stripped ⇒ drop` (a pre-stripped benign string passes unchanged, so "unchanged ⇒ already-sanitized-by-construction" still holds — Q8's reasoning intact).

**RD-10 — No ADR.**
Three-of-three fails everywhere: render/chip/tag decisions are trivially reversible; the prompt 2→3 bump and the LLM-explains-never-scores boundary are already recorded in the committed source spec (P5/P9); the `mechanism` trace field is additive UNDER the already-landed `"7"` whose carrier (001) set the no-ADR precedent (001 grill RD-9), and trace.py:16 explicitly anticipates additive fields without re-bumping. The ADR 0017 5→6 addendum covered a NEW run-level field + bump; this is neither. CONTEXT.md (entry landed with this grill) is the right register. Checked ADR 0021 for a prompt-version policy — it has none (forward-ledger fields only), so nothing to extend there either.

**RD-11 — Doc-sync refs verified + one stale mention adopted.**
`docs/monitor/README.md` targets confirmed (macro bullet ~line 70, report-anatomy ~line 123). Found in passing: the file still says `eval_trace.json (schema 6)` at lines 75 and 218 — stale since 001 landed `"7"`. AC14 adopts the two one-word repairs (same file, zero scope creep).

**RD-12 — Threading + reconciliation-by-construction verified.**
`bundles` are in scope at the `_write_outputs` call site (built monitor_cmd.py:991-1002, call at :1050) and the SAME objects feed `_write_eval_artifacts → build_eval_trace` (:1036), so AC6's render/trace equality holds by construction. `_narrative_dump`'s `__macro__` block (`monitor_cmd.py:445-454`) fits the additive `mechanism` key; `narrative.json` verified production-reader-free (writer + tests only).
