Fix rounds: 0

## Exit contract — satisfied
The three post-ship verdicts all pass:
- verify       → PASS            (items/001-verify.md)
- review       → PASS-WITH-NITS  (items/001-review.md; /ship steps 8+9)
- pr-review    → PASS-WITH-NITS  (items/001-pr-review.md; /code-review on PR #84)

Zero unresolved blockers, zero latent correctness bugs. The one real latent bug
(adversarial A1: NaN leaking through `consensus_upside_pct`'s guard) was fixed
pre-push during /ship (commit 12d5560, +2 tests) — so no fix round was required
after ship.

## Accepted nits (NOT fixed — cosmetic, do not block per the autodev contract)
1. `akshare_index_valuation.py` broad `except Exception` in `_fetch_frame` —
   accepted-by-precedent (matches existing fetchers; safe-degrade; live test is the
   drift detector). Recorded in `TODOS.md` → Reliability for follow-up.
2. `akshare_index_valuation.py:96-100` repeated `x if x is not None else pd.DataFrame()`
   ternary (×3) — cosmetic.
3-4. `tests/opportunity/test_inputs_loader.py` — two "must-not-be-called" guards
   (`_boom`, generator-throw lambda) are unreachable because the production code
   early-exits before the fetch. The tests still assert the correct output via the
   early-exit path; the guards are redundant, not wrong. Flagged by both reviewers.
5. `tests/fundamentals/test_akshare_index_valuation.py` — `_today_iso` unpatched
   where `as_of_iso` is unasserted (inert).

Rationale for not fixing: these are cosmetic/test-hygiene nits on an inert
infrastructure item; spinning a fix round + full re-verify is not justified.
Item (1) is tracked in TODOS; items (3)-(4) are worth a cleanup if this area is
touched again (noted for item 002, which extends the same valuation path).
