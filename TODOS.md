# TODOS

Known gaps and deferred work. Updated after v0.5.0.0 ship (2026-05-11).

## Security

- [ ] **SSRF via feedparser redirect** — `rss_aggregator.py` DNS-guards the initial hostname but `feedparser.parse()` follows HTTP redirects internally, which could bypass the guard. Mitigation: pass a custom urllib handler with no-redirect policy or pre-resolve final destination. Low-priority since feed URLs are operator-supplied. (adversarial-review 2026-05-11)
- [ ] **Cross-class correlation check** — `correlation_filter.py` only drops intra-class correlated instruments; highly correlated equity + QDII pairs can survive. Add optional cross-class check. (adversarial-review 2026-05-11 INVESTIGATE)
- [ ] **Float tolerance edge case** — IEEE 754 sums for YAML-parsed floats like 0.3+0.4+0.3 can exceed 1e-4; `schemas/inputs.py` uses `_TARGETS_TOLERANCE = 1e-4`. Verified OK for current `preferences.yaml` (sum=1.0 exactly). Monitor if users add new targets. (adversarial-review 2026-05-11 INVESTIGATE)
- [ ] **Accurate per-fund manager tenure** — `ingest_cmd._ingest_active_fund_tenure` uses inception-date proxy; replace with a real per-fund manager-start-date field from EastMoney or a dedicated tenure API.

## Reliability

- [ ] **`evals/architecture/metrics.py` silent PASS on syntax errors** — files with syntax errors trigger `continue`, appearing to have no imports and hiding potential DAG cycles. Surface as WARN.
- [ ] **`evals/scoring/runner.py` false stability on 1-item corpus** — `len(scores)==1` produces empty split; `score_distribution_stability([], [x])` returns `0.0`, classified as PASS. Return WARN when split has <2 observations.
- [ ] **Mixed-fund stale-cache with empty `fund_level_evidence` not force-retried** — when `_fetch_active_fund_level_evidence` returns `()` (e.g. NAV fetch failed once), and the fund's CN constituents satisfy `_active_snapshot_has_required_data_leg_gap`, the snapshot is cached with empty evidence. Next run reuses the cached snapshot; rule 2.5 emits `foreign_heavy_fund_level_evidence_missing` for up to `IRC_CACHE_FRESHNESS_DAYS` (default 7) until a full refetch is triggered. Add a freshness probe: `if fund_level_evidence == () AND _compute_foreign_listed_share(...) >= FOREIGN_HEAVY_THRESHOLD: force refetch`. (item-001 ship adversarial review 2026-05-26)
- [ ] **`_ak_call` has no timeout enforcement** — AkShare's internal HTTP calls run unbounded. Item 001 adds +4 calls per active fund × ~50 funds ≈ 200 calls of additional exposure. On a network hang, the entire `_build_rows` loop blocks indefinitely. Wrap `_ak_call` (or add per-fetcher timeout) using `signal.alarm` or a `concurrent.futures.ThreadPoolExecutor` with a deadline. (item-001 ship adversarial review 2026-05-26) — **Partial mitigation (2026-06-30):** the launchd wrappers now bound the *whole run* with a wall-clock watchdog (`ops/launchd/lib-run.sh::run_with_watchdog`, `IRC_MONITOR_TIMEOUT`/`IRC_SNAPSHOT_TIMEOUT`) that kills the process group on a no-timeout network hang. This caps unattended exposure but does NOT replace a per-`_ak_call` deadline (a manual `irc monitor` is still unbounded). See `docs/2026-06-30-launchd-watchdog/items/001-spec.md` threat model.
- [ ] **`fetch_cn_index_valuation` broad `except Exception` masks schema drift** — `akshare_index_valuation.py::_fetch_frame` catches all exceptions and returns `None`, indistinguishable from "index not covered". A renamed legulegu PE/PB column or a retired `stock_index_pe_lg`/`stock_index_pb_lg` endpoint would silently null PE/PB forever with no log signal (impact is currently inert; once item 002 consumes pe/pb it degrades valuation to `evidence_insufficient`, a safe failure). Matches the existing fetcher convention; the double-gated live test is the current drift detector. Consider narrowing the catch or emitting a structured debug log on the swallow. (funding-analysis-001 ship silent-failure review 2026-05-31)
- [ ] **`index_valuation_history` stale-cache served without an age signal** — `opportunity/inputs_loader.py::_index_valuation_metrics` reads cached rows with no `_ingested_at` currency check. If the ingest-stage writer fails (best-effort, non-fatal) on a run, a prior run's rows persist and drive `valuation_percentile_fundamental` as if fresh, with no signal to the report. This is the project-wide cached-evidence property (true of prices/nav/holdings/macro too), not specific to this table, so a per-table expiry was deferred out of Phase 1 to stay consistent with the architecture. Lowest-risk mitigation: log/surface the max `_ingested_at` for the index series so operators can see data age. (valuation-grounding-001 ship silent-failure review 2026-06-03)
- [ ] **`_EXCHANGE_FROM_SYMBOL_PREFIX` missing `"5": "SH"`** — Shanghai-listed ETFs starting with `5` (e.g. `510300`) are classified as `UNKNOWN` by `_infer_exchange`. Conservative under-count only (never causes incorrect publish), but cosmetic correctness. Mirror `_parse_exchange_from_ticker` in `akshare_fundamentals.py`. (item-001 ship adversarial review 2026-05-26)
- [ ] **monitor `009225` index valuation not lit — `china_internet` not an index-valuation key** — `irc monitor`'s index-anchored `valuation` factor (`monitor/valuation.py`) honestly ships `valuation_no_anchor` for `009225` (天弘中证美互联网QDII) because its `tracked_index` `china_internet` is not a member of `opportunity/lookthrough._INDEX_VALUATION_KEYS` (it's a QDII-US display name, not a recognised index-valuation key with cached `index_valuation_history`). The slice-1 wiring is proven via `csi300`; lighting `009225` requires adding `china_internet` to the index-valuation key set with a real cached PE/PB series source. Out of scope for the valuation-heat slices (no new valuation fetch path — spec non-goal). (monitor-valuation-heat-001 ship review 2026-06-17)
- [x] **monitor idempotency + notifier key on `report.html`, the FIRST of 5 written artifacts** — `run-monitor.sh`'s once-per-day idempotency guard and the `notify-status` success detection both treat `outputs/<date>/monitor/report.html` as the completion sentinel, but `monitor_cmd._write_outputs` writes it FIRST (`monitor_cmd.py:373`), then `signal.json`/`impacts.json`/`narrative.json`/`monitor.json` (last, `:388`). A crash between the `report.html` replace and the `monitor.json` replace leaves a present-but-incomplete output set: the guard then skips the re-run — now a full 24h away under the single daily 12:15 fire, vs the old 13:00 retry — and the notifier may report success on an incomplete run. Harden BOTH consumers to the last-written artifact (`monitor.json`), keeping them consistent. Pre-existing hole, not introduced by the schedule change. (monitor-ops-schedule-flow-best-effort ship silent-failure review 2026-06-30) **Resolved 2026-06-30:** both consumers now key on `monitor.json` (the last atomic write) — `run-monitor.sh` guard (`SENTINEL=`) + `notify_cmd._build_outcome` monitor branch (`today_dir_exists=sentinel.exists()`), with consistency called out in `ops/launchd/README.md` and the plist comment. Partial-output regression guards added in `tests/notify/` + `tests/ops/` (report.html present, monitor.json absent → wrapper must re-run, notifier must classify `failed`). Branch `fix/monitor-completion-sentinel`.
- [x] **monitor per-symbol soft-throttle (empty 200 body) still mints a same-day miss** — `monitor/cached_fetch.py` treats only a *raised* fetch as `transient` (retried, not cached). If EastMoney soft-throttles by returning an empty/garbled HTTP-200 body instead of raising, `industry_valuation._classify_industry` → `parse_stock_industry` None → `DEAD` (cached miss), and `flow_fetch._classify` → empty series → `ok` rows=[] (cached) — both not retried that day. The 2026-06-21 throttle empirically *raised* (failed symbols were `status:miss`, and empty-parse routes to `ok`), so transient=raised matches the observed mode and this residual is documented in ADR 0019's 2026-06-22 addendum. Mitigation if it bites: detect a throttle/error sentinel in the empty frame and classify it `transient`. (monitor-fetch-transient-resilience ship adversarial review 2026-06-22) **Resolved 2026-06-22:** `_classify`/`_classify_industry` now split *no usable field* into blank-frame (None/empty/missing-columns → `transient`, retried) vs content-absence (well-formed frame, field genuinely absent → `dead`); needs no throttle sample, preserves the well-formed-no-`行业`→DEAD path. See ADR 0019 "two per-symbol cache residuals" addendum.
- [x] **monitor cache: an unknown `status` string deserializes to a permanent same-day miss** — `cached_fetch` (and origin) map any non-`ok` cache entry to `None`; because the symbol IS present in `cached`, it is served as a confirmed miss and never refetched that day. Only `ok`/`miss` are ever written, so this can only arise from external corruption / a manual edit, but it is a poisoning hole the transient-resilience work does not close. Mitigation: treat an unrecognised status as cache-absent (refetch) rather than miss. (monitor-fetch-transient-resilience ship adversarial review 2026-06-22) **Resolved 2026-06-22:** `_load_cache_payload`/`_load_industry_cache` now OMIT any entry whose status is outside `{"ok","miss"}` (with a WARN) → reads as cache-absent → refetch. Same ADR 0019 addendum.
- [ ] **Surface synthetic `qdii_premium_pct=0.0` for off-exchange feeders** — `qdii_premium_for_row` returns synthetic `0.0` for `market="cn_off_exchange"` (they transact at NAV); the decision-report's "QDII premium" display shows `0.00%` indistinguishably from a measured `0.0` for an on-exchange ETF trading exactly at NAV. Add a synthetic-vs-measured flag (or annotation) so the display can say e.g. `(NAV-priced, no fetch)`. (item-002 ship adversarial review 2026-05-26)
- [ ] **`lru_cache` test isolation pattern** — `_fetch_full_etf_spot_table` uses `lru_cache(maxsize=1)`; tests already follow the `cache_clear()` teardown pattern. When future contributors add tests touching the bulk fetcher, ensure the same `try/finally: _fetch_full_etf_spot_table.cache_clear()` pattern is followed. Document the requirement in a contributor note under `tests/data/`. (item-002 ship 2026-05-26)
- [ ] **`valuation_fundamental_signal` computed twice per row** — `opportunity/states.py` calls it inside `classify_valuation` and again in `build_opportunity_row` before `compose_opportunity_state`. Deterministic on the frozen `OpportunityInput` (no correctness bug; drift PASS validated the single-threading-param design), but the two call sites could drift if the signature changes. Consider extracting a shared local binding once `classify_valuation`'s signature can take the signal explicitly. (funding-analysis-002 ship silent-failure review 2026-05-31)
- [ ] **`_NOTCHABLE_VALUATION_STATES` includes `"cheap"` (no-op arm)** — `opportunity/states.py`; the one-notch cheap-direction adjustment only does real work on `reasonable_low`. The `"cheap"` membership is a harmless no-op but reads as if it moves the state. Naming/comment cleanup only. (funding-analysis-002 ship code review 2026-05-31)
- [ ] **NaN/inf in pe/pb/consensus_upside render literal `nan%`/`inf%` in the reason string** — `opportunity/valuation_fundamental.py`; pydantic permits NaN/inf and they flow into the caveat text cosmetically. No state impact (NaN→neutral, +inf→cheap, -inf→rich) and data sources don't emit them. Add input screening only if a source is found to produce them. (funding-analysis-002 ship adversarial review 2026-05-31)
- [ ] **`rich` reason branch renders a signed percentage** — `opportunity/valuation_fundamental.py:66` emits `隐含 -30% 下行` (signed value + 下行) while the neutral-negative branch was fixed to use `abs()` → `下行空间 30%`. Not factually wrong (both convey downside) but stylistically inconsistent; `test_reason_phrase_rich_mentions_downside` currently locks the `-30%` form. Align the rich branch to `下行空间 {abs():.0%}` and update that test for consistency. (funding-analysis-002 pr-review /code-review 2026-05-31)
- [ ] **Verify AkShare `净资产收益率` (ROE) unit: ratio vs percent** — `fundamentals/ratios.py` / `akshare_filing.py::_profitability_metric` assume ratio units (0.18 = 18%); the test fixture uses 0.18. If the live `stock_financial_abstract` returns percent-scale (18.5), item 004's `abs(roe) > 1.5` guard currently degrades it to None (safe: no wrong display, but ROE never shows). Confirm the live unit via a double-gated test (`-m live_akshare` + `IRC_RUN_LIVE_AKSHARE=1`); if percent-scale, add a `/100` normalisation instead of degrading. (funding-analysis-004 ship review 2026-05-31)
- [x] **`盈利能力`-section schema drift silently yields `roe=None`** — `akshare_filing.py::_profitability_metric` uses `df.get("选项")`/`df.get("指标")`; a renamed section (`盈利能力`→…) makes the mask all-False → `roe=None` with NO `failure_reason` appended and no log, indistinguishable from "legitimately absent". Matches the existing `_common_metric` convention and the spec ("ROE absence never fails the digest"), so acceptable — but means AkShare section-drift is unobservable in production logs. Consider a structured debug log on the swallow (same family as the item-001 `fetch_cn_index_valuation` broad-except TODO). (funding-analysis-004 ship silent-failure review 2026-05-31) **Resolved 2026-06-30:** `_profitability_metric` now emits a `_log.debug` (naming the 盈利能力 section + metric + column) on the empty/absent-column path; degradation behavior unchanged. Test `test_profitability_section_absent_emits_debug_log`.
- [ ] **`_profitability_metric` screens NaN but not ±inf (defense-in-depth)** — `akshare_filing.py:129` uses `math.isnan` while `ratios.py::_finite` was upgraded to `math.isfinite`. A ±inf ROE would reach `FilingDigest.roe` but is fully screened by `compute_ratios._finite()` before any display, so harmless today. Align `_profitability_metric` (and the parallel `_common_metric`) to `math.isfinite` for consistency. (funding-analysis-004 pr-review /code-review 2026-05-31)
- [ ] **`tushare` is a HARD dependency** — `pyproject.toml` lists `tushare>=1.4` under `dependencies`, forcing a ~60 MB install (lxml/requests transitively) on every `uv sync`, including token-absent / CI deployments where Tushare is never used (lazy-imported only inside `_tushare_call`). Move it to `[project.optional-dependencies]` as a `tushare` extra; guard `default_cn_provider()` to warn (not crash) if a token is set but the package is absent. (funding-analysis-003 ship code review 2026-05-31)
- [x] **Tushare column-level schema drift silently → `None`** — `tushare_provider.py` mappers (`_map_index_dailybasic` pe/pb/dividend_yield candidate-sets, etc.): a renamed/missing Tushare column within an otherwise-successful frame degrades that field to `None` with no log (call-level failures now warn after the 003 fix, but column-level drift inside a good frame does not). Same family as the item-001/004 schema-drift TODOs. Consider a debug log when an expected column is absent. (funding-analysis-003 ship silent-failure review 2026-05-31) **Resolved 2026-06-30:** new `_resolve_col` wrapper (keeps pure `_first_col` pure) DEBUG-logs the field name + candidate set when no candidate column is present; wired into `_map_fina_to_digest` and `_map_index_dailybasic`. Test `test_map_fina_missing_roe_column_emits_debug_log`.
- [ ] **Failure-reason key changes on the Tushare-token path** — with `TUSHARE_TOKEN` set, `FallbackProvider` swallows the AkShare exception before `_evidence_for_constituent`'s outer `except` fires, so a connectivity fault is recorded as `filing_empty`/`broker_empty` instead of `filing_fetch_failed`/`broker_fetch_failed`. No output-correctness impact (no downstream consumer keys on the distinction; `advisory_gaps` only checks `broker_empty:*`) and dormant until a token is set — but it degrades operational observability of connectivity faults. The 003 swallow-logging mitigates this (the swallow now WARNs); revisit if the failure-reason key is ever consumed. (funding-analysis-003 pr-review adversarial 2026-05-31)
- [ ] **Tushare `_INDEX_TS_CODE` covers only 4 of 9 broad indices** — `tushare_provider.py`; `csi1000`, `csi_a500`, `star50`, `csi_dividend`, `csi_dividend_lc` have no Tushare ts_code mapping, so `fetch_index_valuation` Tushare fallback is inert for them even with a valid token (AkShare still serves them; degrades to None only if AkShare also misses). Add the remaining ts_codes when validated against the live API. (funding-analysis-003 pr-review /code-review 2026-05-31)
- [ ] **`IndexValuation.dividend_yield` unit convention undocumented** — `tushare_provider.py:~167`; both AkShare and Tushare paths agree today (raw percentage points), but the `IndexValuation` type has no comment stating the unit. Add a one-line unit note to the type to prevent future drift. (funding-analysis-003 pr-review /code-review 2026-05-31)
- [ ] **`tushare_provider.py` is 234 lines (over the <200 ideal)** — grew with the 003 swallow-logging fix. Cohesive (provider impl + pure frame→DTO mappers + `_tushare_call` edge + ts_code helpers) so not urgent, but consider extracting the pure mappers into a `tushare_mappers.py` if it grows further. (funding-analysis-003 verify/pr-review 2026-05-31)
- [ ] **`assert` as a production precondition in `valuation_fundamental.py:61`** — `_fundamental_reason_phrase` has `assert inp.consensus_upside_pct is not None`, which `python -O` strips, so the precondition vanishes in optimized runs. The caller guard (`if fundamental is not None:` in `states.py`) already guarantees it, so the assert is redundant; replace with an explicit early-return guard or drop it (item-002 code, surfaced by item-003's round-3 /code-review). No correctness impact in normal execution. (funding-analysis-003 pr-review /code-review 2026-05-31)
- [ ] **Eager top-level import of `debate.py` in `opportunity_cmd.py`** — `from irc.opportunity.debate import ...` is imported unconditionally even on flag-OFF runs. Declarative (no LLM call, no side effect, no behavior change), but a minor "effects at edges" smell; could move the import inside the `if debate_route is not None:` block. (funding-analysis-005 pr-review adversarial 2026-05-31)
- [ ] **`test_renderer_is_deterministic` only asserts same-call stability** — `tests/opportunity/test_debate.py`; it compares `compose_thesis_debate_markdown(d) == compose_thesis_debate_markdown(d)` in-process (trivially true). The cross-artifact determinism is actually covered by `test_canonical_artifacts_byte_identical_with_vs_without_flag`, but the named test should assert against a literal expected string to be meaningful. (funding-analysis-005 pr-review /code-review 2026-05-31)
- [ ] **16-hex citation test in `test_debate.py` uses a 3-hex stub** — `test_debate_file_introduces_no_citation_id` feeds `[ref:abc]` (3 hex) not a real 16-hex id. `thesis_debate.md` is not in the SAME-3/canonical set so a citation-looking string there cannot contaminate the citation contract, but the test would not catch a real 16-hex passthrough. Strengthen the fixture to a real 16-hex id. (funding-analysis-005 pr-review adversarial 2026-05-31)
- [ ] **`run_debates` outer `except Exception` has no log** — `opportunity/debate.py:~144`; the outer guard is unreachable in practice (the inner `run_defend`/`run_falsify` already swallow+log per the 005 fix), so it's a dead safety net, but for consistency with the FIX-A logging pattern it should `_log.warning` if it ever fires. Low priority. (funding-analysis-005 pr-review /code-review 2026-05-31)
- [ ] **Duplicate `FalsificationResult` class name** — `opportunity/debate.py:~31` defines a card-shaped `FalsificationResult` distinct from `research/falsification.py`'s theme-shaped one. ADR 0011 records this as intentional (different shapes, no reuse), but the shared name is a collision footgun for future readers/importers. Consider renaming the card-shaped one (e.g. `CardFalsificationResult`). (funding-analysis-005 pr-review /code-review 2026-05-31)
- [ ] **`ActiveFundSnapshot` thesis path lacks the dual-leg coverage check** — `derive_thesis_from_evidence` (`opportunity/states.py`) sets `thesis_state="intact"` for an `ActiveFundSnapshot` whenever flattened constituent evidence is non-empty, *without* requiring both a `data` leg and an `information` leg (the dual-leg gate exists only on the `FundLevelSnapshot` branch). A snapshot with data-only (e.g. filing-only) evidence can therefore reach `intact` → and, with cheap valuation + cold heat + acceptable quality, `core_dca`. Pre-existing (not introduced by eval-funds), but `irc eval-funds` surfaces `core_dca` prominently, so a data-only-evidence false-confidence is now more visible. Consider extending the dual-leg check to the `ActiveFundSnapshot` branch. (eval-funds ship adversarial review 2026-06-01)
- [x] **`load_active_fund_cache` swallows `OSError`/`ValueError` without logging** — `fundamentals/snapshot_cache.py:~242` returns `None` on a missing OR corrupt/permission-denied cache file with no log line, so a corrupt cache is indistinguishable from "not yet fetched" in `irc eval-funds` output (both render `missing_constituent_snapshot`). Emit a debug/warn log on the swallow so corrupt caches are diagnosable without filesystem inspection. (eval-funds ship silent-failure review 2026-06-01) **Resolved 2026-06-30:** `load_active_fund_cache` now WARNs (with the path) on an unreadable file, a non-object JSON body, AND a well-formed-but-corrupt snapshot (`_active_fund_from_dict → None`) — logging lives at the I/O edge so `_active_fund_from_dict` stays pure. Tests `test_load_active_fund_cache_warns_on_unreadable_json` / `_on_corrupt_snapshot`. Also covers the `load_latest_active_fund_cached` corrupt-swallow entry below (same loader).
- [ ] **Rule-2.5-exempted constituent failures absent from `citation_audit.json`** — `find_incomplete_constituent_analyses` skips rule-2.5 (foreign-heavy) publishable rows wholesale (`opportunity_cmd.py` Step 2b), so their pure-failure constituents never reach `constituent_findings` in the shadow log. They stay visible in the `## 持仓明细` appendix (rendered `❌`), but an operator grepping `citation_audit.json` for fund data-health sees an exempted foreign-heavy fund as clean even when a *CN-side* constituent genuinely (vs. structurally-unreachable HK/US) failed. Consider an `exempted_constituents` field on the shadow payload for audit-JSON completeness. (rule-2.5-gate-exemption ship silent-failure review 2026-06-04)

## Coverage gaps

- [ ] **Monitor look-through valuation coverage gap (item 002)**: look-through `valuation` for the 6 pure active funds depends on `stock_valuation_history` coverage of their *constituents*. Today that table is populated only for **watchlist-overlapping A-shares** (`irc fundamentals snapshot` enumerates `fund_holdings.holding_ticker` via `_discover_ashare_codes`); there is NO dedicated monitor-constituent stock-valuation ingest. So a monitor fund whose top A-share holdings don't overlap the discovered watchlist ships `valuation_no_anchor` (honest N/A), and HK/US (QDII) holdings never match. **Follow-up (non-goal of this spec):** add a monitor-constituent stock-valuation ingest keyed off `ActiveFundSnapshot` symbols so the look-through lights up regardless of watchlist overlap.
- [x] **Unlogged corrupt-snapshot swallow in `load_latest_active_fund_cached`** — `src/irc/fundamentals/snapshot_cache.py:198-206`/`240-246` catch deserialization errors and return `None` with NO log line, so a *corrupt* `ActiveFundSnapshot` JSON is indistinguishable from a legitimate cache-miss. Monitor look-through (item 002) and the constituent factor both newly depend on this: a corrupt cache silently degrades to `valuation_no_anchor` instead of surfacing a warning (the item-001 `resolve_valuation_state` try/except never fires because the loader pre-swallows rather than raises). Pre-existing, observability-only (no wrong output — N/A is honest either way); same family as the other "broad except masks X" entries. **Fix:** emit a structured WARNING (with the file path) on the swallow so corrupt-cache faults are diagnosable. (monitor-valuation-heat-002 ship silent-failure review 2026-06-17) **Resolved 2026-06-30:** `load_latest_active_fund_cached` calls `load_active_fund_cache`, which now WARNs on every corrupt/unreadable path (see entry above).
- [ ] `config_loader._resolve_schema` — `KeyError` path when schema is not registered (100% happy-path only)
- [ ] `settings.py` — `OPENROUTER_API_KEY` missing path
- [ ] `schemas/triggers.py` — invalid comparator branch (~50% coverage)
- [ ] `schemas/overrides.py` — populated override lists (~50% coverage)
- [ ] `schemas/gold.py` — direction enum variants beyond "up"
- [ ] `schemas/discovery.py` — quality filter edge cases

## Design / Tech debt

- [x] **`write_reason` silent failure**: bare `except Exception: pass` swallows all LLM errors; `run_discover` returns 0 even when 0 candidates found. Add structured logging for retried/failed instruments. (adversarial-review 2026-05-08) **Already resolved (verified 2026-06-30):** `discovery/reason_writer.py::write_reason` now `_log.warning`s every failure path — per-attempt failures, post-retry give-up (with provider/network hint), the no-raw_refs skip, and the ungrounded-citation skip. No bare swallow remains.
- [x] **`fetch_fund_metadata` wrong record on miss**: falls back to `df.iloc[0]` when fund_code not found, returning metadata for a different fund. Raise `ValueError` or return `{}` instead. (adversarial-review 2026-05-08) **Already resolved (verified 2026-06-30):** `akshare_client.py:fetch_fund_metadata` now raises `FundNotFound` when the code is absent from the full table and `ValueError` when the XueQiu basic-info code doesn't match; no `df.iloc[0]` fallback remains.
- [ ] **Opportunity venue filtering not wired**: `OpportunityInput.venue_compatible` is always `True`; wire it from `bundle.account.venues` so venue-incompatible instruments are routed to `small_watch` instead of `core_dca`. (code-review 2026-05-14)
- [x] **Opportunity valuation/heat/product fields not wired from ingest**: `_build_input` always sets `valuation_percentile_self`, `drawdown_since_entry`, `expense_ratio`, `aum_cny`, `manager_tenure_years`, and momentum/flow fields to `None`. Wire from ingest output when those feeds are available. Until then, virtually all states degrade to `evidence_insufficient`. (code-review 2026-05-14) **Completed: v0.8.2.0** — `inputs_loader.populate_inputs()` now reads rolling returns, drawdown, percentile, expense ratio, AUM, manager tenure, tracking error, premium/discount, and flow from DuckDB.
- [ ] **`reduce_same_index` per-index backup not in default pipeline**: `reduce_same_theme` Stage 1 keeps only one best representative per index key (no backup). `reduce_same_index` in `selection.py` is available for callers that need primary+backup, but is not wired into `run_opportunity`. Evaluate whether exposing a backup per-index improves display or fallback logic. (code-review 2026-05-14)
- [x] **`_bucket_rows` silent fallback on unknown `dca_action`**: unknown values silently land in "今日可定投" via `.get(..., default)`. Add a log warning or raise `KeyError` to surface new `dca_action` values that are missing from `_DCA_BUCKET`. (code-review 2026-05-14) **Resolved 2026-06-30:** `_bucket_rows` (`opportunity/report.py`) now WARNs (naming the value + instrument) when `dca_action` is outside `_DCA_BUCKET`, while preserving the historical 今日可定投 default. Test `test_unknown_dca_action_warns_and_falls_back`.
- [x] **`demote_unstable_active` treats `theme=None` as a shared bucket**: all instruments with `theme=None` share one passive-quality bucket; a single unrelated passive instrument can demote every active fund that also has `theme=None`. Guard with `if r.theme is None: continue` in the best-passive scan, or treat `None` as unmatched. (adversarial-review 2026-05-15) **Resolved 2026-06-30:** the best-passive scan now skips `r.theme is None`, so a `None` key is never populated and `theme=None` active funds are never demoted by unrelated themeless passives. Regression test `test_themeless_active_not_demoted_by_unrelated_themeless_passive`.
- [ ] **Verbatim traceability check is LLM-whitespace-sensitive**: `ref in memo_text` requires exact substring match; LLM models (especially CN-routed) may insert soft line breaks or normalize whitespace, causing legitimate citations to score 0. Consider normalizing both sides before comparison, or treating this as a known false-negative. (adversarial-review 2026-05-17 INVESTIGATE)
- [ ] **`geopolitical_stress` token false-positives**: EN tokens use word-boundary regex (`\b`), so `"war"` does not match "backward"/"reward" and `"attack"` does not match "attacking"/"attackers". The remaining semantic false-positive is `"strike"` → "strike price" (matched as a whole word in financial texts). CJK tokens still use plain substring matching but are short enough to be unambiguous in practice. Low severity; acceptable given the intentionally-simple design. Revisit if score quality degrades. (adversarial-review 2026-05-17 INVESTIGATE)
- [ ] **`signal_consistency` hardening (NaN/Inf + missing-key + renorm tolerance)**: `src/irc/monitor/eval/structural.py` — three pre-existing soft spots surfaced while fixing the 4dp rounding artifact: (1) a NaN `composite` (or ±inf-cancelling contributions) PASSes because `abs(nan - x) >= _EPS` is always False — add a `math.isfinite` guard → FAIL; (2) `sig.get("composite", 0.0)` / `c.get("contribution", 0.0)` default a *missing* key to `0.0`, so a malformed/truncated `eval_trace.json` signal block can pass silently — assert key presence → FAIL; (3) `sum_renorm` is still compared full-precision against `1.0` at `_EPS = 1e-9` (the same false-positive class just fixed for composite, left on the adjacent line) — round to a consistent precision if it ever fires spuriously. All below-threshold today (data sources don't emit NaN/inf; renorm float error is ~1e-16). (ship reviews — code-reviewer / silent-failure-hunter / adversarial 2026-06-17)

## Completed

- [x] **SSRF DNS-bypass**: DNS-resolve at call time in `http_client._verify_host_resolves_publicly` + applied to `rss_aggregator`, `ldr_client`. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **Remaining plain-str secrets**: `SecretStr` for anthropic/tushare/ldr/fmp/tiingo tokens in `Settings`. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **Two-hop prompt injection**: raw refs sanitized before auditor prompt in memo pipeline. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **Unbounded question length**: `MAX_QUESTION_LEN = 2000` guard in `ask_cmd`. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **No aggregate timeout / deadline_s dead code**: outer loop now catches `httpx.HTTPError`; deadline_s is live. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **`sign==0` always "downtrend"**: returns `'neutral'` on zero-slope. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **`compute_gold_score` KeyError on config drift**: explicit key validation added. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **Correlation filter permanently disabled**: activated with intra-class renormalization. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **`ChatResponse.raw` unbounded**: bounded via opt-in env flag; default `None`. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **Portfolio target tolerance**: tightened from ±2% to 1e-4. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **`FailureKind.OK` dead code**: removed. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **Tenacity decorator rebuilt per call**: bound at module level. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **Sequential LLM calls in discovery**: parallelized `write_reason` with `ThreadPoolExecutor`. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **`fetch_fund_metadata` / `fetch_etf_metadata` cache**: `lru_cache` on full-table fetches. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **`tracking_error` stub**: rolling TE vs role benchmark now live in `discovery/metrics`. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **2/6 gold score drivers hardcoded**: `cb_purchases` + `etf_holdings_30d` wired from WGC CSV. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **`traceability.py` exact-copy lower bound**: replaced with token-based fuzzy coverage. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **`traceability.py` misleading coverage_ratio**: token-overlap heuristic wrong for CN text; replaced with verbatim-count schema (`n_refs_provided`, `n_refs_quoted_verbatim`). **Completed:** v0.8.4.0 (2026-05-17) — PR #19.
- [x] **Decision gate: legacy schema and empty-pool block-all**: `compose_decision_report` now handles legacy `memo_traceability.json` (no `n_refs_quoted_verbatim` key) and empty evidence pool (`n_refs_provided=0`) without silently blocking all decisions. **Completed:** v0.8.5.0 (2026-05-17)
- [x] **`geopolitical_stress_0to1` hardcoded at 0.4**: wired from persisted `geopolitics` theme report via `geopolitical_stress_from_theme_report`. Degrades to 0.4 when report absent or failed. **Completed:** v0.8.5.0 (2026-05-17)
- [x] **Mixed-date fallback in memo**: warn when scoring/gold/allocation inputs span mixed dates. **Completed:** v0.5.0.0 (2026-05-11)
- [x] **Sequential LLM calls in scoring**: parallelized with `ThreadPoolExecutor`. **Completed:** v0.3.0.0 (2026-05-08)
- [x] **`ingest` aborts on single instrument failure**: changed to skip-and-warn. **Completed:** v0.3.0.0 (2026-05-08)
- [x] **Bond ETF quality filter `_is_active_fund` heuristic**: consolidated into `instrument_kind.requires_manager_tenure`. **Completed:** v0.4.0.0 (2026-05-10)
- [x] **Resilient CN exchange price fetch**: EastMoney primary with automatic Sina finance fallback. **Completed:** v0.4.0.0 (2026-05-10)
- [x] **Role-aware allocation top-K**: two-phase greedy ensures role diversity. **Completed:** v0.4.0.0 (2026-05-10)
- [x] **Negative tenure from future inception date**: rejects ≤0 years. **Completed:** v0.4.0.0 (2026-05-10)
