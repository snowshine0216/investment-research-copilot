Verdict: PASS

Subagent: sonnet
Source: /verify (superpowers verify skill, cold-start — no project-level `.claude/skills/verify` found; entry point already known: CLI `irc notify-status`)
Entry point exercised: `uv run irc notify-status --run-kind {monitor|weekly|flow-capture} --last-exit-code 0 [--no-notify-on-clean] [--repo-root <tmp>]`, run directly against (a) the real symlinked `outputs/`/`data/` repo root for AC1 and AC2-A (today, 2026-07-07, real wall clock confirmed via `datetime.now(timezone(timedelta(hours=8)))`), and (b) tmp-scaffolded repo roots (mktemp -d, cleaned up after each check) built from real committed fixtures (`tests/notify/fixtures/health/*`) or real artifact files copied under the real `today` (2026-07-07) date-dir for the sub-cases that don't naturally occur in the retained real window (ok-after-ok, ok-after-abstain, DARK, corrupt). Every AC was confirmed via the real CLI's log line (`notify-status severity=... notify=...`, plus process exit code where relevant) AND a same-dispatch Python harness calling the identical `notify_cmd._build_outcome` / `classify_run_outcome` functions the CLI calls, to inspect exact body text (the CLI itself does not print body text, only severity/notify to the log).

Observed behavior:
  - AC1 (monitor, real 07-07 artifacts) — `uv run irc notify-status --run-kind monitor --last-exit-code 0` (real repo root) logged `severity=degraded notify=True`. Harness confirmed body: `Run completed; nothing actionable. · 板块PE: STALE-1 (07-06) · 资金流: 最新 07-07 · 覆盖 29/30 · 1 只滞后>3td(最旧 06-26)`. Matches the plan's documented judgment call (001-plan.md "Spec-gap judgment calls" #2): board-PE STALE stays info-only, but the real flow store's per-symbol stale count (688072 stuck at 06-26) escalates today's real severity to `degraded`, not `clean`.
  - AC2-A (flow-capture, rotation abstain) — real today's artifact (`outputs/2026-07-07/rotation/rotation_radar.json`) is itself `data_status: abstain`; ran `uv run irc notify-status --run-kind flow-capture --last-exit-code 0 --no-notify-on-clean` against the real root: logged `severity=degraded notify=True`. Harness body: `轮动雷达: 弃权 (连续第 1 日)` (streak=1 because real 07-06 was `ok`, breaking the streak at index 1).
  - AC2-B (ok-after-ok, silent) — scaffolded tmp root with real `ok` rotation content (from 07-06) mapped to both today and yesterday date-dirs; CLI logged `severity=clean notify=False` (no dispatch); harness confirmed body unchanged (`Run completed; nothing actionable.`, no health lines) and `should_notify is False`.
  - AC2-C (ok-after-abstain, recovery) — scaffolded tmp root with real 07-06 (`ok`) mapped to today and real 07-05 (`abstain`) mapped to yesterday; CLI logged `severity=clean notify=True`; harness confirmed `outcome.recovery_notice == "轮动雷达恢复 ok (200 boards) — 此前弃权 1 日"` and decision body equals that string with `should_notify is True` — the one-time recovery notice fires despite `--no-notify-on-clean`.
  - AC3 (weekly, real 07-04 gold_regime.json) — scaffolded tmp root with the real `outputs/2026-07-04/gold_regime.json` copied to today's date-dir; ran the real CLI (`--run-kind weekly`); harness body contained `宏观驱动: DXY 滞后 21d (06-16) · 缺失驱动: etf_holdings_gld` verbatim.
  - AC4 (board-PE DARK fixture, `IRC_NOTIFY_ON_CLEAN=0`) — scaffolded tmp root from committed fixtures `eval_trace_dark.json` + `fund_flow_series.json`; ran `IRC_NOTIFY_ON_CLEAN=0 uv run irc notify-status --run-kind monitor --last-exit-code 0 --repo-root <tmp>` (real env var, real CLI): logged `severity=degraded notify=True` — confirms `degraded ∈ _ALWAYS_NOTIFY` overrides the clean-suppression env var. Harness body contained `板块PE: DARK ≥4td — 价值陷阱检测不可用`.
  - AC5 (corrupt `eval_trace.json`) — scaffolded tmp root with `eval_trace.json` = `"{bad json"`; ran the real CLI without piping so the true process exit code was captured: `REAL_EXIT_CODE=0`, log `severity=degraded notify=True` — no crash, no exception, run rc not masked. Harness body contained `数据健康未知 (health unknown)`.
  - AC6 (docs synced) — read all 4 target files directly: `docs/adr/0016-local-scheduling-and-notification.md` has an "Amendment (2026-07-07): data-health digest, `degraded` severity, `flow-capture` run-kind" section (lines 100-125) describing the new severity, precedence, health.py, and flow-capture run-kind; `ops/launchd/README.md` line 9 (15:45 row) states "notifies only on degradation/abstain/failure via `notify-status --run-kind flow-capture --no-notify-on-clean` ... + a one-time abstain→ok recovery notice", and the timeout table (lines 65-67) documents the capture-timeout-pages-failed behavior change; `docs/monitor/README.md` line 44 (15:45 daily row) documents the full notify-status flow-capture tail behavior (silent-ok / degraded / failed / recovery notice); root `README.md` line 221 documents the `com.irc.flow-capture` row with the same degradation/recovery language. All 4 doc edits are present and consistent with the spec.

Findings (verify-skill "push on it" probes):
  - 🔍 Probed AC5's exit code directly (not through a pipe, since piping through `tail` masks the real process `$?`) — confirms `notify-status` truly exits 0 on a corrupt companion artifact, not merely "didn't crash visibly."
  - 🔍 Noted that AC2-A's real consecutive-abstain count today is 1, not the spec's illustrative "连续第 2 日" example — this is expected (07-06 between the two abstain days was `ok`), not a defect; the counting logic itself was independently confirmed correct.
  - AC3's harness body additionally showed a pre-existing, unrelated "Sell-side state unknown (stale artifact) — re-run `irc opportunity`" base message (from `classify.py:91`, predates this feature) because the tmp scaffold only supplied `gold_regime.json`, not a full weekly output set — this is base classifier behavior on an incomplete synthetic weekly scaffold, not a 001 regression; the health line (`DXY 滞后 21d`) appended correctly alongside it, which is itself good evidence that health-append works uniformly across pre-existing severities per spec §3.2.
  - All tmp scaffolds (5 total: AC2-B, AC2-C, AC3, AC4, AC5) were created via `mktemp -d` and `rm -rf`'d immediately after use; `git status --short` at the end of this session shows a clean working tree — no stray files.
  - No Feishu webhook is configured in `.env`; all `should_notify=True` cases in this session fired a local macOS notification only (osascript), consistent with the stated harmless/expected side effect.

Failures: none
