Fix rounds: 1

## Triage of the 3 post-ship verdicts

| Verdict | Result | Action |
|---|---|---|
| verify (`/verify`) | PASS | none needed |
| review (`/ship` 8+9) | PASS-WITH-NITS | 1 P1 latent (empty calendar → false PASS) FIXED pre-merge `6789abd` |
| pr-review (`/code-review` #162) | PASS-WITH-NITS | 3 nits, 0 bugs |

## Fixes applied

- **`6789abd`** (pre-merge, from /ship review) — empty/all-NaT AkShare frame or poisoned
  `{"dates": []}` cache no longer yields a false `nav_quality` PASS: `fetch_trade_calendar` raises on
  empty, `_read_cache` treats empty `dates` as a miss. +3 regression tests.
- **`20f9016`** (pr-review nit 1) — `_fetch_and_persist` return annotation `frozenset[date] | None`
  → `frozenset[date]` (it can no longer return None after `6789abd`).

## Accepted (not fixed)

- pr-review nit 2 — `_missing_trading_days` O(calendar × pairs): negligible at `window=20` /
  ~5k calendar entries. Accepted.
- pr-review nit 3 — `monitor_cmd` uses `date.today()` not the resolved `_today`: matches spec §3.4
  (`load_trading_days(date.today())`) and drift-verified; only affects cache-refresh cadence at a
  back-dated replay (harmless — calendar is full SSE history). Accepted.

Loop-exit contract met: all 3 post-ship verdicts PASS / PASS-WITH-NITS; zero blockers, zero
remaining latent bugs. Post-fix: ruff clean; trading_calendar + monitor suites green.
