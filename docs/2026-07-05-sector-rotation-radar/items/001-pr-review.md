Verdict: FAIL

Source: /code-review on PR #205
PR comment URL: https://github.com/snowshine0216/investment-research-copilot/pull/205#issuecomment-4885269606
Findings: 4
  - src/irc/rotation/board_fetch.py:81 (parse_board_hist) — latent-bug — Off-by-one on kline CSV parsing: fields2 requests exactly 8 fields (f51..f58, indices 0-7 per docs/2026-07-05-sector-rotation-radar/items/001-probe-notes.md's own field table and tests/rotation/fixtures/board_hist_sample.json), but turnover is read via `_f(parts[8]) if len(parts) > 8 else None`; len(parts) > 8 is always False, so every backfill/kline-sourced BoardDay.turnover_pct is unconditionally None — contradicting composite.py's comment that turnover is "structurally always populated." Effect: for up to _KEEP_TD=60 trading days after any `irc rotation seed`, a board's turn_delta silently defaults to 0.0 (via _tail_mean returning None) with no data_status/diagnostics flag — the same dark-factor failure shape D6/flow_leg_dark was built to prevent, just unflagged and on the turnover leg. Bounded/self-healing, does not break AC3/D6/AC11, no test coverage of turnover_pct on backfill rows today.
  - src/irc/rotation/_cmd_helpers.py:81 (_load_holdings_cache) — nit — uses cache filename (fund_id) for both fund_id and name_cn; real Chinese fund name never recovered from cache, so report tables show fund code duplicated in the name column. Display-only.
  - src/irc/rotation/types.py:63 (RotationReport.diagnostics) — nit — `field(default_factory=dict)` is a mutable field on an otherwise-frozen dataclass; no mutation call site found, so not an active bug but an immutability smell vs. CLAUDE.md FP conventions.
  - src/irc/rotation/seed.py:27-54 (seed_boards) — nit — one full read-modify-write of board_series.json per board (~86 rewrites/seed run) rather than one batched write; atomic and resumability-safe, efficiency-only.

Verified correct (not findings): D6 flow_dark is a genuine global decision (composite.flow_leg_dark + rotation_cmd's all-today-None OR, tested by test_flow_leg_dark_prevents_fabricated_zero_flow and test_partial_flow_gap_triggers_global_flow_dark); AC3 byte-stable determinism holds (sorted keys/rows, atomic writes, NaN/inf-rejecting _f(), store-not-refetch PE read, tested by test_same_day_rerun_byte_identical); AC11 one-way dependency holds (zero irc.rotation references under src/irc/monitor/, self-enforced by tests/rotation/test_import_isolation.py); AC10 wrapper chaining in ops/launchd/run-flow-capture.sh is genuinely protective-only (separate radar_rc, exit "$rc" untouched); AC2 seed resumability holds (skip-if-present + per-item try/except + atomic writes). 77/77 tests pass, ruff clean, wrapper shell test PASS — all reproduced locally. PR-claimed pre-push fixes (NaN/inf guard, ledger key mom20, visibly-logged calendar/topup fallback) all verified present in the diff.
