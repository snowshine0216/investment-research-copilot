Verdict: PASS

Subagent: sonnet
Items reviewed: 3 (001, 002, 003 — merged); item 004 out of scope for functional coverage (env-paused, not merged; only its pre-committed design docs checked for false as-built claims)

Doc changes verified:
- `CHANGELOG.md` — three "[Unreleased]" entries added, one per merged item: divergence caveat detail (003), self-explanatory caveats + weekly LLM-suite auto-refresh (001), macro direction chips + strength tags + mechanism clause (002). Each states schema/engine-version impact correctly (003: no schema/engine bump; 001: schema 6→7, no engine bump; 002: additive under existing "7", no second bump, prompt 2→3).
- `CONTEXT.md` — 今日速览 entry updated with the item-001 fourth/first-position run-global caveat row (matches `render_overview.py` intent per 001-grill RD-5); new term **Mechanism clause (传导线) / macro direction chips** added for item 002 (absence≠zero, required-optional mechanism, LLM-explains-never-scores, 归因未知 tag); **Validation badge / chip** entry updated with the anchor+tooltip clause (item 001); new term **Caveat reason (run-global vs fund-specific)** added (item 001, RUN_GLOBAL_STAGES classification + age-stamp-at-source rule); **Flow freshness state** entry updated to mark `_provisional_flow_note` as now wired (pre-existing v3 work, not part of this run — verified unrelated to items 001-003 but not a regression).
- `docs/adr/0020-monitor-dual-track-valuation.md` — new addendum (item 004 design, board-PE three-state staleness + batch-first industry names) correctly scoped: "Accepted" refers to the design decision, not shipped code; cross-references item 004 explicitly.
- `docs/monitor/README.md` (ops manual) — step 6 narrative description updated for prompt v3 + mechanism clause (002); step 7 schema 6→7 (001); §"Open the report" walkthrough gains direction-chip/strength-tag/mechanism-line description (002); weekly-wrapper section gains the eval-refresh best-effort paragraph + idempotency-sentinel edge case (001); maintenance table's "Monthly-ish (paid, manual)" row correctly rewritten to "Weekly, automated (Saturday wrapper, best-effort)" with manual-fallback command retained (001); output-table eval_trace row schema 6→7 (001). All three merged items' functional changes are covered; no item-004 (f127/board-PE/industry_map_store) language leaked into this operational doc.
- `docs/diagrams/monitor-workflow.html` — narrative box updated (prompt v3, ≤60字传导, 方向chips由impacts定); eval-spine box updated (⚠ caveated带原因, schema "7"); artifact list + footer schema "6"→"7"; irc eval box updated ("Sat wrapper auto" replacing "live-gated, paid"-only wording); 今日速览 bullet in the legend/notes box gains the run-global caveat row. Fully in sync with 001/002; no item-004 industry-fill nodes added (correctly deferred).
- `evals/README.md` — schema_version "5"→"7" (should read as the cumulative current value; verified trace.py `SCHEMA_VERSION = "7"` matches); narrative metrics list gains `mechanism_validity` (FAIL <0.80) matching item 002's new `metrics_narrative.py` metric.
- `ops/launchd/README.md` + `ops/launchd/run-weekly.sh` — env-var table gains `IRC_WEEKLY_EVAL_TIMEOUT` row; prose section documents the post-notify best-effort eval refresh and the load-bearing `env` prefix fix (RD-3 defect fix). Diffed against the actual script: `run_with_watchdog "${IRC_WEEKLY_EVAL_TIMEOUT:-900}" env IRC_RUN_LIVE_LLM_EVAL=1 "$UV_BIN" run irc eval monitor_impact` (and `monitor_narrative`) both present, matches docs exactly, including default 900s and non-paging/rc-preserving behavior.
- `README.md` — no diff (none required; the top-level README does not carry monitor operational detail per the existing "docs/monitor/README.md is THE monitor ops manual" convention).

Special checks (item 004 pre-merge docs, not yet built):
- CONTEXT.md *Board-PE freshness state* and new *Stock-industry map (cross-day store)* both close with "(Grilled + locked 2026-07-03, report-v4 ... item 004)" — a design-time tag, consistent with how the rest of CONTEXT.md distinguishes "shipped" vs "grilled/spec'd" work. Neither claims the code exists; no "as-built"/"shipped" language used for item 004 behavior.
- ADR 0020 addendum status line reads "Accepted (report-v4 explainability spec, OD-1 resolved at the source grill; hardened at the item-004 grill)" — ADR-decision-accepted, not code-shipped; explicitly named as item 004's design.
- Confirmed via `git diff main...HEAD --stat -- src/` that no industry_map_store.py, board_pe fetch-first reorder, or f127 code changes exist on this branch — the code is genuinely unmerged, matching the docs' design-only framing.
- No merged-surface doc (CHANGELOG, docs/monitor/README.md, evals/README.md, monitor-workflow.html) references f127/board-PE staleness/industry_map_store as already shipped.

Missing coverage: none
