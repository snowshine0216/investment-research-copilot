# Item 004 — WS-4 Industry fill (P7 + P8)

**Run:** monitor-v4-explainability · **Source:** MASTER-SPEC item 004 / source spec §1 problem 4, §2 P7+P8+P9, OD-1 (resolved), §3 WS-4 slices 1–5, §4 bullets 4–5, §5
**Size:** medium-large (touches fetch edges) · **Schema:** additive fields under the EXISTING `"7"` (bump was carried by item 001 — `src/irc/monitor/eval/trace.py:17` comment already anticipates this item; NO second bump) · **No `_ENGINE_VERSION` change** (P9: data-availability class, ADR 0019 precedent)

## Goal

Fill the two dead industry surfaces in the daily monitor brief (`outputs/<date>/monitor/{report,drilldown}.html`), whose failure evidence as of 2026-07-03 is: `data/monitor/stock_industry/2026-07-03.json` holds **7 ok of ~60** requested symbols (per-symbol `stock/get` throttles out; per-day cache resets tomorrow so coverage never accumulates), and `data/monitor/industry_pe/` has **no cache since 2026-06-30** (board `clist/get` empty/failing 07-01→07-03; empty parses are correctly not cached per D3), so every row gets `industry_no_data` and `industry_cover = 0.0`.

Two locked mechanisms:

- **P7 — 行业 names go batch-first.** Extend the ONE already-working `ulist.np` batch call (fields `f12,f14,f184` → `+f127`) at **both existing call sites** — the 12:15 brief's provisional-flow call (`monitor_cmd._provisional_flow_note` → `flow_batch_fetch.fetch_flow_today_batch`, call site `monitor_cmd.py:219`) and the 15:45 flow-capture job (`run_flow_capture`, call site `monitor_cmd.py:1127`). Parsed `f127` industries merge into a **new cross-day store** `data/monitor/stock_industry_map.json` (per-symbol `{industry, seen_at}`, refresh-on-seen, serve-while-stale ≤ 30 d, byte-stable atomic writes, corrupt-file degrade-to-empty), owned by a new `src/irc/monitor/industry_map_store.py` mirroring `flow_series_store.py` patterns. `holding_metrics` consumes the merged map; the per-symbol `stock/get` path (`industry_valuation.fetch_stock_industry_map`, per-day cache + `cached_fetch` 3-outcome/breaker) is kept **only as fallback** for symbols absent from batch responses — in steady state it fires ~never, eliminating the ~60 push2 calls/day that are also the throttle pressure killing the board-PE fetch.
- **P8 — board-PE resilience.** (a) The one paginated `clist/get` board-PE fetch is reordered to run ONCE at `run_monitor` level BEFORE any remaining per-symbol storm (today it hides inside per-fund `_build_full_basket_metrics`, `monitor_cmd.py:329`, so after fund 1's storm every retry fights a heated throttle). (b) On empty/failed fetch, serve the most recent cached day ≤ 3 **trading days** old with an explicit age tag `板块PE 引用 <date> · N个交易日前` and a three-state freshness marker (FRESH / STALE-N / DARK — the *Board-PE freshness state* contract already in CONTEXT.md). Per OD-1: **STALE-N feeds factor math ≤ 3 td; > 3 td → DARK** → per-stock `industry_no_data`, `val_score == self_score`. (c) The 15:45 capture job also best-effort fetches board PE in its rested window so next morning's fallback is at worst 1 day old.

The dual-track valuation math (`_dual_track.py`, weights, clamp) is untouched — this item changes what data reaches it and how honestly its age is labelled.

## Acceptance criteria

Each independently verifiable. File/line references verified 2026-07-03; implementer re-verifies before editing (001–003 land first).

### P7 — batch-first 行业

1. **`parse_ulist` carries f127.** `flow_batch_fetch.parse_ulist(payload)` returns `dict[str, tuple[float | None, str | None]]` — per f12-symbol `(f184_flow, f127_industry)`. f184 coercion is the existing `_coerce` (percent-points, NO /100); f127 → stripped non-empty string, else None (missing key / `"-"` / `""` / whitespace → None — never fabricated). Tolerant of list/dict `diff` shapes; blank/missing data → `{}`. Fixture tests: f127 present / absent / blank / whitespace-only; and a **flow-identity test** — for any fixture payload, the flow halves equal the pre-change `parse_ulist` output exactly (guards the flow_reconciliation byte-identity contract at the parse level).
2. **One call, both maps.** `fetch_flow_today_batch(symbols)` requests `fields=f12,f14,f184,f127` and returns `(flow_by_symbol, industry_by_symbol)` (two plain dicts; every requested symbol present in both, None when the endpoint returned no row/field). Still exactly ONE HTTP GET via `resolve_cn_proxy` — no new call, no pagination. Both production call sites updated in this item.
3. **Secids widen to the full-basket union; the flow store's scope does not.** A new `monitor_cmd` helper `_full_basket_union_symbols(funds, root)` (union of FULL disclosed baskets across active funds, dedup-ordered) feeds both batch call sites — P7's rationale is filling 行业 for the FULL basket in the same single call. `run_flow_capture` slices the returned flow map to the existing `_capture_union_symbols` (top-5) union BEFORE `append_today`, so `data/monitor/fund_flow_series.json` scope and bytes are unchanged for identical inputs (D5 store scope preserved; flow factor reads top-5 only — `aggregate_flow` slices internally). Test: capture with a full-basket batch result writes NO non-top-5 symbol to the flow store.
4. **New `src/irc/monitor/industry_map_store.py`** (mirror test `tests/monitor/test_industry_map_store.py`), patterned on `flow_series_store.py`:
   - On-disk: `data/monitor/stock_industry_map.json` = `{symbol: {"industry": str, "seen_at": "YYYY-MM-DD"}}`, sorted keys, `ensure_ascii=False`, indent 2, written via `irc.io_utils.atomic_write_text` (byte-stable: same store → same bytes).
   - `load_store(path)` → dict; missing / corrupt / malformed rows → `{}` with a WARNING (never crash the brief; same posture as `flow_series_store.load_store`).
   - Pure `merge_seen(store, today, industry_by_symbol)` → NEW dict (no mutation): every non-None, non-blank industry upserts `{industry, seen_at: today}` — **refresh-on-seen: `seen_at` is refreshed even when the industry string is unchanged**; None/blank entries are skipped (absence ≠ evidence of no industry).
   - Pure `fresh_slice(store, today, max_age_days=30)` → `dict[str, str]` of symbols whose `seen_at` is within ≤ 30 **calendar** days of `today` (see Q4); unparseable `seen_at` rows are dropped, not served.
   - Edge `record_seen(path, today, industry_by_symbol)` = load → merge → write → return merged store.
5. **Both call sites merge f127 into the store, best-effort.** The 12:15 brief (in `run_monitor`, immediately after the batch note fetch) and the 15:45 capture job each call `record_seen` with their batch's industry map. A merge/write failure is logged and never crashes the brief or capture (existing degrade posture). Tests at both sites.
6. **Consume order: store → batch → per-symbol fallback.** `_build_full_basket_metrics` receives the run-level serving map (`fresh_slice` of the store AFTER today's batch merge, so today's batch is already in it) and calls `fetch_stock_industry_map` ONLY for full-basket symbols absent from the serving map; its results merge into the HoldingMetrics industry map for this run AND into the cross-day store via `record_seen` (see Q3). The per-symbol per-day cache (`data/monitor/stock_industry/<date>.json`) and `cached_fetch` 3-outcome/breaker/backoff semantics are untouched. Test: with a fixture batch covering the full basket, 行业 is non-None for EVERY holdings row and the per-symbol fetch fake is **never invoked** (source-spec §4 bullet 4); with one symbol absent from batch, only that symbol reaches the fallback.
7. **No renderer/table shape change for 行业.** `holdings_board_html`'s 行业 column and the trace `holding_metrics.rows[].industry` fill from the wider map with zero schema change; `HoldingMetric`/`StockValuation` fields unchanged.

### P8 — board-PE fetch-first + serve-while-stale

8. **Fetch-first reorder.** The board-PE fetch runs ONCE per run in `run_monitor`, before the per-fund loop (hence before any per-symbol fallback call and after/adjacent to the one batch call, which is not a storm). The existing `load_trading_days(...)` call (`monitor_cmd.py:1028`) is hoisted above the loop and its frozenset threaded to BOTH the board-PE age computation and the existing `_compute_gates`/trace uses (still exactly one call per run; it is per-day cached). `(industry_pe_table, board_pe_freshness)` threads through `_process_fund` → `_build_full_basket_metrics`; a direct caller passing neither gets the current lazy-fetch behaviour (mirrors the `flow_slice` library-robustness fallback at `monitor_cmd.py:905-907`). Test: a call-order recorder proves the board-PE fetch is invoked before the first per-symbol fallback fetch across a ≥ 2-active-fund run.
9. **`fetch_industry_pe` gains freshness + stale fallback.** New signature returns `(table, BoardPeFreshness)` where `BoardPeFreshness` is a frozen dataclass `{state: str, as_of: str | None, age_td: int | None}` with `state ∈ {"FRESH", "STALE", "DARK"}`:
   - today's cache exists, or today's fetch parses non-empty (then cached, D3 unchanged — empty parses still never cached) → `FRESH` (`as_of = today`, `age_td = 0`);
   - otherwise scan `cache_dir` for the newest readable `YYYY-MM-DD.json` dated before today; if its trading-day age N ≤ 3 → serve that table with `STALE` (`as_of = <date>`, `age_td = N`) — **this table feeds factor math** (OD-1);
   - nothing readable within ≤ 3 td, or trading calendar unavailable (see Q5) → `DARK` (`{}`, `as_of/age_td` from the newest cached day if any, else None).
   The staleness scan + trading-day age live in a new small module `src/irc/monitor/board_pe_staleness.py` (pure; mirror test file) so `industry_valuation.py` (already 207 lines) does not grow materially. NEVER raises; fetch stays injectable.
10. **Age semantics (locked).** `N = |{trading days d : as_of < d ≤ today}|` computed from the threaded cached calendar — no new calendar source. `FRESH` iff `as_of == today`. A non-trading-day rerun can yield `STALE` with `N = 0` (tag still names the date — honest). Boundary tests: N = 3 serves (STALE), N = 4 → DARK; weekend/holiday gaps count 0 trading days.
11. **Trace marker, no bump.** `build_eval_trace` gains a run-level kwarg → key `"board_pe_freshness": {"state", "as_of", "age_td"} | None` (None when the caller doesn't pass one — additive back-compat, same pattern as `macro_narrative`). `SCHEMA_VERSION` stays `"7"` — asserted unchanged by test; the trace.py comment block is extended to record this field landing under 7.
12. **Panel freshness state.** `eval/structural.valuation_coverage_health` gains an optional `board_pe_freshness: dict | None = None` kwarg; when present it appends one reason — `board_pe FRESH` / `board_pe STALE-2 (as_of 2026-06-30)` / `board_pe DARK` — to the existing 观测 row (status stays PASS; panel-only, never a gate). `_compute_gates` threads the run-level value. Absent kwarg (old traces / other callers) → no reason appended. Unit tests per state + back-compat.
13. **Reader-facing age tag on both surfaces.** New pure helper in `render_drilldown.py` (e.g. `board_pe_age_note_html(freshness)`) renders `板块PE 引用 <date> · N个交易日前` (HTML-escaped, muted styling) when state is STALE, and `""` for FRESH / DARK / None (FRESH needs no noise; DARK already surfaces as `industry_no_data` + the existing 行业覆盖/价值陷阱检测 notes — no new DARK string, KNOWN_NA_REASONS vocabulary unchanged). Rendered adjacent to each holdings board on BOTH surfaces: `render_html._drilldown_block` (report card) and `valuation_rollup_html`/`drilldown_section_html` (standalone drilldown page, threading via the existing variable-length `views` row pattern in `drilldown_page_html`). The freshness reaches the card via a new trailing-defaulted `FundView` field (precedent: run-global `provisional_flow_as_of` is already carried per-view). Render tests: tag present with exact text on a 2-td-stale fixture, absent when FRESH (source-spec §4 bullet 4).
14. **Capture-job board-PE (P8c).** `run_flow_capture` best-effort calls `fetch_industry_pe` (same cache dir, its own `today`) AFTER the flow append; any exception is logged and the job still returns its normal rc (a board-PE failure never fails capture; the watchdog posture stays protective-only). No change to `ops/launchd/run-flow-capture.sh` (the wrapper already runs `irc monitor flow-capture`; the new fetch rides inside the command). Test: raising board-PE fetch → capture rc 0, flow store still written.

### Cross-cutting

15. **`flow_reconciliation` byte-identical; live spot-check is a merge precondition.** No edit to `eval/structural.flow_reconciliation` or any f184 value path (AC-1's parse-level identity test is the unit guard). Before merge: ONE live spot-check through `IRC_CN_PROXY` comparing f184 values for the same secids with `fields=f12,f14,f184` vs `f12,f14,f184,f127` — 4 dp identical (a small throwaway script or extension of `scripts/phase0_flow_batch_spike.py` methodology; NOT a committed live test). If live endpoints are unreachable at that point, this item **pauses at its verify gate as a documented environmental stop** (MASTER-SPEC known risk; items 001–003 land regardless — never silently skipped, never merged unverified).
16. **Engine + reason-vocabulary freeze.** `_ENGINE_VERSION` untouched; `KNOWN_NA_REASONS` untouched (`industry_no_data` remains a per-stock HoldingMetric reason covering DARK; factor-level reasons unchanged); no weight/band/threshold change anywhere.
17. **Docs (slice 5).** (a) ADR 0020 gains an addendum section documenting the board-PE three-state staleness contract + OD-1 resolution (STALE-N feeds factor math ≤ 3 td) and the fetch-first reorder; (b) CONTEXT.md *Board-PE freshness state* term (already landed at the source-spec grill) is verified accurate against the as-built behaviour — fixed inline if any wording drifted (expected drift: none; the term already matches this design); (c) `docs/monitor/README.md` ops manual updated (行业 batch-first + cross-day store, board-PE stale serving + age tag, capture job now also refreshes the industry map and board PE); (d) `docs/diagrams/monitor-workflow.html` synced (batch call carries f127 → industry map store node; board-PE fetch-first + freshness states). Per the standing docs-sync convention.
18. **TDD + hygiene + signature-change sweep.** Tests first (red → green); mirror files: `tests/monitor/test_flow_batch_fetch.py`, `tests/monitor/test_industry_map_store.py` (new), `tests/monitor/test_board_pe_staleness.py` (new), `tests/monitor/test_industry_valuation.py`, `tests/monitor/test_render_drilldown.py`, plus `tests/commands/test_monitor_cmd*.py` / `test_monitor_flow_capture.py` / `test_monitor_constituent.py` as touched. `uv run ruff check src tests` clean; `uv run pytest tests/monitor/` green; **`tests/commands/` run per-file only** (whole-dir hangs — known suite-ordering issue). Because `fetch_flow_today_batch`, `parse_ulist`, `fetch_industry_pe`, and `_build_full_basket_metrics` change signature/shape: grep `tests/` for ALL callers of each and run every containing file (standing lesson — the mirror dir alone has missed broken callers before). New/touched source files stay < 200 lines (new modules absorb growth; `industry_valuation.py` must not grow past its current 207), functions < 20 lines ideal.

## Non-goals

- **No flow-leg STALE-N.** The flow factor stays as-built FRESH/abstain-only (ADR 0019); this item's three-state serving applies to board PE only. No change to `flow_series_store` semantics, `_FLOW_KEEP_TD`, D6 completed-day-only writes, or the 盘中提示 annotation.
- **No batch pivot for the per-symbol flow-series leg** (`flow_fetch.fetch_flow_series` / `fund_flow/` seed path) — separate shelved spec.
- **No `_ENGINE_VERSION` bump, no `schema_version` bump** (P9 — the 6→7 bump was item 001's), no forward-ledger shape change.
- **No new calendar source** — the cached SSE calendar (`monitor/trading_calendar.py`) is the only trading-day authority; no per-market calendars.
- **No board-PE methodology change** — single 市盈率 column, cap-weighting unverified (ADR 0020 denominator-robustness risk stands); no alternative PE source.
- **No KNOWN_NA_REASONS additions**, no dark-factor counts in 数据健康 (spec §5), no purge/backfill of historical `stock_industry/` or `industry_pe/` per-day caches (they age out naturally).
- **No 限购/heat, macro, divergence, or caveat-chip changes** (items 001–003 own those surfaces).
- **No launchd plist/wrapper edits** — P8c rides inside the existing `irc monitor flow-capture` command; no agent reinstall required by this item.
- **No industry-name localization/normalization** beyond whitespace-strip — 东财 names are served verbatim (they key `industry_pe` lookups; normalizing one side would break the join).

## Constraints

- **Effects at edges.** New store I/O lives in `industry_map_store.py` edge functions + `monitor_cmd`; staleness scan I/O in `board_pe_staleness.py`'s thin edge; `merge_seen`/`fresh_slice`/age math/tag rendering are pure and unit-testable without mocks. No live network in unit tests — `http_get`/`fetch`/`sleep` stay injectable throughout (house pattern).
- **Immutability.** Pure helpers never mutate arguments (`merge_seen` returns a new dict); frozen dataclasses for `BoardPeFreshness`; trailing-defaulted additive fields only (`FundView`).
- **Determinism/byte-stability.** Store writes are sorted-key, atomic (`atomic_write_text` / `.tmp.{pid} → os.replace`); same inputs → identical bytes; renderer additions are pure and locale-free (ADR 0004 posture).
- **Degrade, never crash the brief.** Every new edge path (store read/write, staleness scan, board-PE fallback, capture-job board-PE) catches, logs WARNING, and degrades to the current behaviour (empty map / DARK / no tag).
- **Proxy + throttle posture unchanged.** All push2 traffic keeps `resolve_cn_proxy` routing; `cached_fetch` breaker/backoff/pacing constants untouched; total push2 volume per brief DROPS (1 batch + ≤ 10 board pages + ~0 fallback vs today's 1 + ≤ 10 + ~60).
- **Back-compat.** Old eval traces (no `board_pe_freshness`) evaluate unchanged; `_process_fund` and `fetch_industry_pe` direct callers without new args get current behaviour; the 15:45 capture job remains safe to run standalone.
- **No new dependencies.** stdlib + existing pandas/requests only.
- **Ops note (post-merge, not a code AC):** first production brief after merge should show 行业 filled full-basket and a board-PE freshness state in the panel; the MASTER-SPEC post-merge ops list covers verification.

## Open questions resolved during brainstorming

Autonomous session 2026-07-03 (no user in loop — resolved from MASTER-SPEC, source spec, ADR 0019/0020, CONTEXT.md terms, and current code; upstream locks NOT reopened). Every cited line/shape was re-verified against the working tree.

- **Q1 — Return shape of the extended batch fetch?** `parse_ulist` → `dict[str, tuple[float | None, str | None]]` (the slice-plan wording "(flow, industry) per secid" taken literally); `fetch_flow_today_batch` → `(flow_by_symbol, industry_by_symbol)` two-dict tuple. A per-symbol record dataclass was rejected as ceremony for a 2-field edge payload; keeping the flow map a plain `dict[str, float | None]` preserves `append_today`'s input shape byte-for-byte.
- **Q2 — Which symbols enter the batch secids?** The FULL-basket union (new `_full_basket_union_symbols`), at BOTH call sites. Top-5-union secids would leave the ~30-symbol basket tail permanently "absent from batch" → permanent per-symbol fallback → the storm P7 exists to kill. Still ONE call (`ulist.np` multi-secid is a single request — phase0 spike-verified). The flow store append is sliced back to the top-5 union so D5 store scope, file bytes, and `flow_reconciliation` inputs are unperturbed.
- **Q3 — Do per-symbol FALLBACK results also merge into the cross-day store?** Yes. The locked rule only mandates batch merging, but the root cause being fixed is "per-day cache resets tomorrow so coverage never accumulates" — a fallback-served symbol (e.g. one the batch never returns) would otherwise be re-fetched per-symbol every single day. The store is source-agnostic `{industry, seen_at}`; both sources write the same record shape.
- **Q4 — Is the ≤ 30 d store window trading days or calendar days?** Calendar days. Industry membership is quasi-static (the rule's own rationale); trading-day precision buys nothing at a 30-day horizon and would couple the store module to the calendar edge. The ≤ 3 td board-PE window, by contrast, is locked in trading days (OD-1) and uses the threaded calendar.
- **Q5 — Board-PE age when the trading calendar is unavailable (`load_trading_days` → None)?** DARK — serve-while-stale is disabled. The no-silent-stale contract requires an accurate `N个交易日前` tag AND OD-1 gates factor-math eligibility on N ≤ 3; without a calendar neither can be computed honestly. Calendar-day approximations (conservative ≤ 3 natural days, or a 自然日-worded tag) were rejected: they add vocabulary for a rare compound failure (calendar fetch is a different host, per-day cached). `val_score == self_score` keeps the valuation factor alive in that case.
- **Q6 — FRESH definition and the STALE-0 edge?** `FRESH` iff `as_of == today` (matches CONTEXT.md "today's board fetch parsed"). A manual rerun on a non-trading day serving yesterday's table yields `STALE` with `N = 0` — the tag still names the source date, so honesty holds without a fourth state.
- **Q7 — Where does `board_pe_freshness` live in the trace, and how does the panel see it?** Run-level trace key (one table per run — per-fund duplication would contradict the run-global-dedupe posture item 001 just established), added via a `build_eval_trace` kwarg like `macro_narrative`. Because `_compute_gates` feeds per-fund projections to the health functions (run-level keys don't reach them), `valuation_coverage_health` takes the value as an optional kwarg threaded from `monitor_cmd` — old traces and existing callers degrade to no reason string.
- **Q8 — Is optional P8c (capture-job board-PE) in or out?** In, as best-effort. It is ~10 paginated calls in the proven rested window (the flow-capture job exists precisely because 15:45 fetches succeed when 12:15 ones throttle), and it converts tomorrow's worst case from DARK to STALE-1. Failure isolation (AC-14) keeps the capture job's contract unchanged, so the marginal risk is nil.
- **Q9 — Where does the serve-while-stale logic live, given `industry_valuation.py` is already 207 lines (> 200 budget)?** New `board_pe_staleness.py` module (pure scan/age/state) + a thin orchestration change inside `fetch_industry_pe`. Relocating the whole board-PE leg out of `industry_valuation.py` was rejected as refactor churn beyond the item's scope.
- **Q10 — `run_monitor` needs trading days before the per-fund loop but currently loads them after (line 1028)?** Hoist the existing call above the loop and thread the frozenset to both consumers — `load_trading_days` is per-day cached and idempotent, so this is a pure move, still one call per run.
- **Q11 — Does changing `parse_ulist` break the phase-0 spike surface?** No — verified `scripts/phase0_flow_batch_spike.py` has its own private `_parse_ulist` (urllib-based, deliberately decoupled) and `tests/scripts/test_phase0_flow_batch_spike.py` tests that private copy. No edit there.
- **Q12 — Where must the reader-facing age tag render?** Both surfaces that show 行业PE numbers: the report card's holdings board (`render_html._drilldown_block` — the phone-visible surface; verified it does NOT currently include `valuation_rollup_html`) and the standalone drilldown page. One shared pure helper prevents wording drift. Per-board (not once-per-page) placement is deliberate: unlike item 001's run-global caveat chips, the tag labels numbers physically present in each board — adjacency is labelling, not repetition.
- **Q13 — How does the run-global freshness reach the card renderer without render-signature churn?** A trailing-defaulted `FundView` field, following the existing precedent of run-global render context carried per-view (`provisional_flow_as_of`, `purchase_tag`).
- **Q14 — Does widening `_provisional_flow_note`'s symbols perturb the 盘中提示?** No — `_provisional_flow_for_fund` reads only each fund's top-5 symbols from the returned map; extra keys are inert. The note's wrapper is renamed/reshaped to return both maps (flow half for the annotation, industry half for the store merge) while keeping its best-effort None-on-failure contract.

**None unresolved from docs + code.** The single execution-time unknown is environmental, not a design question: whether live EastMoney endpoints are reachable for the AC-15 f184 spot-check at merge time (MASTER-SPEC already defines the pause-at-verify-gate handling).
