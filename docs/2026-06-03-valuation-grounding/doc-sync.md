Verdict: PASS

Files changed in run: 34
Doc files already changed: 0 (confirmed via `git diff main...feat/fundamental-valuation-grounding -- 'CONTEXT.md' 'docs/adr/**' 'README.md'` — empty; this feature shipped zero glossary/ADR/README updates, as expected for a grill-skipped spec)
Findings: 6
Findings auto-resolved: 6
Findings requiring human review: 0

## Findings

- **F1 — `index_valuation_history` + `fetch_cn_index_valuation_history` undocumented.** New DuckDB table (registered in `EXPECTED_TABLES`), AkShare-only full-series fetcher (vs `fetch_cn_index_valuation`'s latest-row-only), and the ingest-stage writer `index_valuation_ingestor.ingest_index_valuation_history`. Verified in `git show 655427b` against `duckdb_helper.py`, `akshare_index_valuation.py`, `index_valuation_ingestor.py`, `ingest_cmd.py`. Added a new CONTEXT.md entry after the `IndexValuation` entry, cross-linking it; recorded R4 (NOT a provider method) and R3 (only source the opportunity stage reads). Auto-resolved.
- **F2 — `valuation_percentile_fundamental` (+ `_pb`) undocumented.** Primary equity anchor: index PE-TTM historical percentile via `self_history_percentile` (≥30 pts, rank-inclusive, `<30 → None`); `classify_valuation` bands on it when present (cheap<.20·reasonable_low<.40·fair<.70·expensive<.90·very_expensive≥.90), else byte-for-byte NAV fallback; PB corroboration-only (Q5). Broad-index CN only (9 `_BROAD_INDEX_KEYS`). `populate_inputs` reads cached table, no live fetch (R3, `provider` retained-unused). `earnings_yield`/`real_yield_10y` lit in ratio units (R1). Verified against `types.py`, `inputs_loader.py`, `states.py`, `returns.py:59`. New CONTEXT.md entry added. Auto-resolved.
- **F3 — `valuation_price_fundamental_divergence` advisory code undocumented.** `ADVISORY_GAP_CODES` member → `advisory_gaps`, never `evidence_gaps` (H3/SAME-3 untouched). Single pure detector `valuation_divergence_code` (different `_band` tier OR `|gap| ≥ DIVERGENCE_PCT_GAP=0.25`); reason-note in `classify_valuation` (signature stays `(state, reason)`); folded into `combined_gaps` by `build_opportunity_row` via `_divergence_gaps`; surfaced as inline discipline-row suffix `证据缺口：价格与基本面估值背离`. Verified against `advisory_gaps.py`, `states.py`, `report.py`. New CONTEXT.md entry added next to `top_holdings_broker_thin`. Auto-resolved.
- **F4 — Stale "Valuation-input inertness" entry (~line 138).** It asserted `pe_ttm`/`pb` "remain reason-only (no state effect — no self-history/peer percentile context)" — this feature ADDS that context and makes the index PE-TTM percentile DECIDE the broad-index `valuation_state`. Rewrote the entry: index-percentile path inertness now LIFTED (fundamental-led); preserved accurate parts (all-`None` dormancy when no cached rows; `dividend_yield` reason-only; consensus-upside veto unchanged — R5; per-A-share pe/pb still out of scope). Auto-resolved.
- **F5 — ADR assessment.** Applied the three-of-three rule: hard-to-reverse (changes `valuation_state` derivation for the whole broad-index cohort, risk inherits it) ✔ · surprising (PE-percentile overrides NAV; nominal-gap-vs-real-yield + TIPS trap R1; ingest-vs-provider-seam R4; no-live-fetch retained-unused-provider R3) ✔ · real trade-off (fundamental-vs-price; nominal-gap-vs-real-yield) ✔. All three met → wrote `docs/adr/0012-fundamental-led-equity-valuation.md` (Context/Decision/Consequences, matching 0009/0010), cross-linking the design spec + ADRs 0005/0009/0010. NOTE: ADR slot 0011 was already taken (`0011-adversarial-debate-advisory-only.md`); the task brief said "0011" but the next free number is 0012. Auto-resolved.
- **F6 — README "Evidence refresh order".** Step 1 (`irc ingest`) is exactly where `index_valuation_history` refreshes; the section enumerates the refresh chain, so a one-line mention applies. Added to step 1: ingest also refreshes the cached index PE/PB series backing the fundamental valuation anchor (opportunity stage reads, never fetches live). Auto-resolved.
