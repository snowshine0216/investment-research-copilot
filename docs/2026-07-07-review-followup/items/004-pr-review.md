Verdict: PASS-WITH-NITS

Source: /code-review on PR #208
PR comment URL: none (inline findings)
Findings: 3

- src/irc/rotation/_cmd_helpers.py:96-97 (`resolve_candidates`, `name_to_code` build) — nit — `coverage_pct`/`unmapped_syms` now implicitly depend on board maturity: `name_to_code` is built only from this run's `states`, and `composite.board_signals` drops any board with < `MIN_TD` (20) trading days of series history (`src/irc/rotation/composite.py:20,31`). A store-held 行业 name that maps to a not-yet-mature board is counted `unmapped` post-fix (pre-fix it was silently counted `mapped`, just useless for the code-keyed active filter). `items/004-grill.md` Q1 already surfaces this and calls it "moot on the 07-06 proof date (all 200 boards mature)" — i.e. the "coverage byte-identical pre/post" proof (items/004-notes.md) holds for that date only, not as a general guarantee. No test exercises this branch; the gap is effectively covered by the already-deferred `unmapped_syms`-cause-split TODO in `TODOS.md`.
- src/irc/rotation/_cmd_helpers.py:80-91 (`_translation_warnings`, duplicate-name branch) — nit — `name_to_code = {b.board_name: b.board_code for b in states}` keeps only the last `BoardState` per duplicate `board_name` (dict-comprehension last-write-wins); the new warning (commit `b37bc4cb`) logs the collision but does not change the join, so stocks belonging to the shadowed board would misattribute to the surviving code if a real collision ever occurs. Accepted tradeoff, not new: grill-verified 200 collision-free board names on real 2026-07-06 data; revisit only if the warning fires in production (already the documented pickup trigger).
- tests/rotation/test_resolve_candidates.py — nit — no dedicated test for "行业 name present in the store but its board is absent from this run's mature `states`" (distinct from "name absent from the store entirely", which is covered). `items/004-notes.md` pre-triages this as an accepted gap ("same guard clause as the covered unmapped-symbol path").

## Verification performed independently

- Read full diff (`gh pr diff 208`, 9 files: CHANGELOG.md, CONTEXT.md, TODOS.md, 6 docs/ files, `src/irc/monitor/industry_map_store.py`, `src/irc/rotation/_cmd_helpers.py`, `src/irc/rotation/exposure.py`, 3 test files).
- Confirmed the core fix: `resolve_candidates` builds `name_to_code = {b.board_name: b.board_code for b in states}`, translates `stock_to_name` (行业 names, from `fresh_slice(load_store(...))`) to `stock_to_code` with an `if nm in name_to_code` guard, then calls `build_exposure(funds, stock_to_code)` — matches the stated fix (`{board_name: board_code}` translation before the active-board join) and correctly turns the previously-always-empty `candidates` list into real rows.
- Confirmed `build_exposure`'s dead `board_names` param was dropped cleanly (single caller updated, 3 test call sites updated to 2-arg).
- Confirmed the two P1s already fixed pre-push (`b37bc4cb`, per `items/004-review.md`) are present in the diff: `_translation_warnings` logs once for dropped name translations and once for duplicate `board_name`s, both caplog-tested.
- Ran the full affected test surface independently (not trusting prior task-report prose): `uv run pytest tests/rotation/test_exposure.py tests/rotation/test_candidates.py tests/rotation/test_resolve_candidates.py tests/rotation/test_seed.py tests/rotation/test_report.py tests/monitor/test_industry_map_store.py -q` → 34 passed; `uv run pytest tests/commands/test_rotation_cmd.py -q` → 12 passed (ran per-file per FACTS.md/CLAUDE.md convention, never the whole `tests/commands/` dir).
- `uv run ruff check src/irc/rotation src/irc/monitor/industry_map_store.py tests/rotation tests/monitor/test_industry_map_store.py` → All checks passed.
- Confirmed no VERSION/`radar_version`/`schema_version` bump (repo convention: accumulate under CHANGELOG `[Unreleased]`).
- Confirmed `BoardState`/`ExposureRow` remain frozen dataclasses; the translation is a pure dict comprehension (no argument mutation) — consistent with CLAUDE.md functional/immutability conventions. Logging is the only effect, confined to the edge-glue `_cmd_helpers.py` module (effects-at-edges convention).
- No PR comment exists on GitHub (`gh api repos/snowshine0216/investment-research-copilot/issues/208/comments` → 0 comments) — the code-review skill's generic template offered no auto-posting connector for this repo, so all findings above are inline/self-performed rather than a fetched PR comment.

No blockers, no latent bugs found beyond what the PR's own review/adversarial passes already caught and fixed in-branch (`items/004-review.md`). All 3 findings above are nits — either already-accepted design tradeoffs (surfaced in the grill) or documented test-coverage gaps already flagged in `items/004-notes.md` / `TODOS.md`.
