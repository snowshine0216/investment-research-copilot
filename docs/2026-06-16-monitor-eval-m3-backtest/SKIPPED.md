# SKIPPED

None — single-task spec mode (N=1). All scope is IN. M4 work (factor ablation, weight/band sensitivity, the economic-rationale ADR) is a separate future milestone, not a deferred sub-item of this spec.
