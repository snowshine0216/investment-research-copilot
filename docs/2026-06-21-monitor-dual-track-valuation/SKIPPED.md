# SKIPPED — Monitor dual-track valuation

_No items skipped — single IN-scope feature (degenerate N=1 spec mode)._

The post-composite veto tier (conflict hard-suppression, flow-reversal guard, Amihud veto) is **out of scope by design** (spec §10 / ADR 0019 D4 — evidence-gated, separate future spec), not an autodev skip.
