# SKIPPED — Monitor report v4 explainability

No backlog items skipped — all 4 workstreams are IN-scope.

Spec-level non-goals (source spec §5), recorded for the audit trail — these were never backlog items and are not re-tried:

- **Constituent/valuation dark-factor counts in 数据健康** — separate causes; needs its own analysis.
- **"Not-heated" fund scout (WS-C)** — has its own spec (2026-07-02 reliability roadmap); unbuilt by design.
- **Any weight/band change** — engine semantics locked; out of this render/availability-class run.
- **Per-claim LLM direction** — rejected at grill (globally ambiguous, adds injection surface); theme-level mechanism (P5) is the locked replacement.
- **nav_cover backfill (D8 of the light-up spec)** — ops task, not code.
